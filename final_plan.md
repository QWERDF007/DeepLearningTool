# DeepLearningTool 架构改进最终方案

## 0. 文档定位

本文是多方案审查结果收敛后的唯一架构改进方案。它将架构审查、领域拆分、线程生命周期、数据与模型任务、评估性能、界面投影和测试验收收敛为一条实施路线，作为后续重构的决策入口。

本文不替代运行时事实源。具体内容以以下位置为准：

- 模块依赖、构建选项和安装导出：根目录 `CMakeLists.txt`、`src/CMakeLists.txt`、各模块 `CMakeLists.txt`。
- 公开接口、所有权和线程归属：`src/**/include/` 下的头文件及其实现。
- 数据库表、字段、约束和迁移：`src/database/include/database/ddl/`。
- 模型参数、分组、默认值和说明：`config/models/` 以及实际使用的 EasyTrain、Anomalib、Ultralytics 代码。
- 测试注册和执行：`tests/`、`cmake/AddTest.cmake`、`tools/run_model_tests.py`、`tools/run_project_tests.py`。
- 稳定的架构、存储、开发和测试约定：[`docs/README.md`](docs/README.md)、[`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)、[`docs/DATA_MODEL.md`](docs/DATA_MODEL.md)、[`docs/DEVELOPMENT.md`](docs/DEVELOPMENT.md)、[`docs/TESTING.md`](docs/TESTING.md)。

### 0.1 方案输入与裁决规则

本方案的合并对象为 `luna_final_plan.md`、`gemini_final_plan.md` 和 `musespark13_final_plan.md`。当前工作区、项目父目录及可见 Git 历史未发现这三份独立文件，因此本稿以现有综合方案、仓库源码事实和已记录架构决策为可验证输入，不虚构缺失文件的具体内容。统一采用以下裁决规则：

- 源方案只提供问题识别、候选设计和取舍依据；本文件是唯一的实施顺序、验收标准和禁止事项入口。
- 源方案之间存在冲突时，优先采用能够保持单一事实源、明确所有权、缩小接口并可通过行为测试验证的方案。
- 方案中的模块名、状态名和数据字段不构成新的运行时事实源；实现时必须以源码、CMake、DDL、YAML、任务协议和测试注册为准。
- 目标架构不要求一次性重写现有系统；每个阶段必须在一个真实 seam 上完成行为测试、替换实现、删除旧路径并通过 Release/CTest 验证。

阅读本方案时，先以第 1 节确认决策，再按第 8 节实施；第 0 节和第 9 节分别约束事实源和验收方式。本文不记录方案评审过程，也不保存字段、状态或路径的第二份定义。

当本文与源码、CMake、DDL、YAML 或 CTest 注册不一致时，以事实源为准，并在对应事实源稳定后更新本文的索引和决策。

## 1. 统一结论

### 1.1 保留的技术主干

继续使用 Qt 6/QML、C++、SQLite、CMake 和 Python/EasyTrain。保留已经证明有效的结构：

- Project 作为项目级所有权根；
- DataManager、ModelManager、FeatureManager 作为领域入口和 QML 投影入口；
- IEvaluationEngine、评估方法注册表和 EvaluationResult；
- DataIO 的格式扩展点及 COCO、LabelMe、Mask、Folder 实现；
- ModelStorageService 的模型路径和任务目录规则；
- ExternalModelTaskRunner、TaskManager 和 Python 任务协议链路；
- AddTest.cmake、普通测试/项目级测试分离、CTest 执行和项目级分层脚本。

不为了追求类数量、目录数量或形式上的分层而全面重写。每一次拆分都必须对应真实职责、变化轴、所有权问题或可测试的 seam。

### 1.2 不可破坏的原则

1. **单一事实源**：定义、持久状态、原始产物和展示派生数据各有唯一权威来源，其余位置只能是可重建投影。
2. **深模块**：复杂行为集中在少数有深度的模块中，调用方只依赖少量稳定接口；接口同时说明类型、顺序、错误、取消和性能契约。
3. **快照跨线程**：后台只消费不可变纯值、绝对路径、取消令牌和明确的 Adapter，不捕获 GUI 对象、Qt Model、数据库连接或裸 manager 指针。
4. **按职责和变化轴拆分**：结构问题通过重构解决；只有真实的职责边界和测试 seam 才增加模块。
5. **破坏性重构**：不保留旧任务、旧参数组、旧 revision、旧 SQL 入口或长期兼容双轨。新契约不成立时直接报错。
6. **可验证交付**：每个阶段先增加穿过目标接口的行为测试，再替换实现；未验证的结果不能写成已完成。

### 1.3 合并后的方案取舍

最终方案采用“保留有效主干、只在真实接缝处深化”的取舍：

| 议题 | 最终选择 | 不采用的方向 |
| --- | --- | --- |
| 分层方式 | 以 `ProjectContext` 组织项目所有权，以 data、model、feature 的深模块承载领域行为 | 为每个页面、表或函数增加只有一层转发的 facade |
| 异步执行 | 以不可变请求快照、取消令牌、实际句柄和生命周期校验组成 `TaskScope` | 让 worker 捕获 manager、Qt Model、QObject 或跨线程数据库连接 |
| 持久化 | 项目数据库、任务数据库、模型/预测原始产物分别保持唯一事实源，派生结果可重建 | 在 `extra_data`、缓存字段和第二套报告格式中重复保存事实 |
| 数据库 | 版本化 schema 正本、事务和强类型 Record 集中在 database 适配层 | 继续维护多份 DDL、运行时建表 SQL 或机械 Repository |
| 文件与数据库协同 | 模型文件操作使用 staging/quarantine 和可恢复操作状态，再提交逻辑记录 | 把 SQLite 事务误当成能够覆盖文件系统的原子事务 |
| 评估性能 | 预测文件复用、轻量产物身份、评估结果与视觉派生分离、实例按需加载 | 全量 TIFF 像素文本哈希、无界缓存或每次打开都重算视觉结果 |
| 几何一致性 | 一个 `GeometryKernel` 统一导入、评估和 QML 的尺寸、裁剪、padding 与反映射 | 在各格式 Adapter、评估引擎和 QML 中各自实现坐标换算 |
| UI 责任 | QML 只做投影、布局和轻量交互，任务状态与业务规则由 C++ 深模块提供 | 在 QML 中实现数据库事务、协议解析、评估算法或坐标反映射 |
| 迁移方式 | 先补行为测试，再逐接缝替换实现；新契约稳定后删除旧路径 | 以兼容分支、旧字段和双轨接口延长历史包袱 |
| 验收方式 | Release、CTest、项目级分层流程和 full 流程共同验收，并覆盖失败/取消/关闭 | 只依赖编译成功、单个 happy path 或固定代码行数指标 |

上述选择共同保证接口保持小而深：调用方只需提交明确的值对象并消费结果，复杂的线程、事务、文件布局、协议和缓存策略留在实现内部；真正变化的位置通过内部 seam 测试，而不是把实现细节扩散到所有调用方。

### 1.4 合并后的决策顺序

当多个改造目标发生冲突时，按以下顺序决策：

1. 先保证对象所有权、项目关闭和任务终态正确，再优化吞吐量。
2. 先固定事实源和纯值输入，再增加缓存、异步派生和 UI 投影。
3. 先在深模块接口上补行为测试，再移动实现；测试不绑定内部文件布局。
4. 只有真实职责、变化轴或测试 seam 才拆出新模块；禁止用转发类制造形式分层。
5. 明确废弃的旧契约直接删除；不增加旧任务、旧参数组、旧 revision 或旧 SQL 入口的兼容分支。

### 1.5 风险优先级

| 优先级 | 结构问题 | 必须达到的结果 |
| --- | --- | --- |
| P0 | 后台越界访问、取消不收敛、项目关闭后迟到回调 | 所有后台操作有实际句柄、可取消、可等待；关闭后不再触碰旧项目对象 |
| P0 | 文件系统与 SQLite 的模型操作不一致 | 创建、复制、重命名、删除可恢复，不能返回与磁盘状态不符的成功 |
| P0 | Schema 多处手工维护 | 一个版本化 schema 正本，迁移可回滚，未知版本直接失败 |
| P0 | 全局任务状态与项目状态混用 | ApplicationContext、ProjectContext、TaskScope 有明确所有权 |
| P0 | 任务定义、运行状态、预测和展示结果重复写入 | 每类数据只有一个权威来源，投影都可重建 |
| P1 | DataManager、DataIO、数据库和任务编排职责混杂 | 通过少数深模块和内部 seam 收敛变化轴 |
| P1 | C++/Python 协议、终态和进度语义漂移 | 一份契约、双端强校验、边界 fail-fast |
| P1 | 评估重复读取和重复计算导致 UI 卡顿 | 评估输入快照化，预测复用，视觉派生懒加载且可取消 |
| P1 | 坐标转换在导入、评估和 QML 中重复实现 | 一个几何规则，统一处理缩放、裁剪和 padding |
| P2 | 配置、CMake、环境和日志事实分叉 | 配置可注入、构建可诊断、日志有界、测试可分层 |

## 2. 当前架构审查

### 2.1 已有优势

- Project 已承担项目数据库、DataManager、FeatureManager、ModelManager 和任务对象的聚合，QML 不直接管理 SQLite 连接。
- data 已有格式 IO 多态扩展点，继续优化实现 locality 和公共几何规则即可。
- model 已形成模型记录、任务准备、外部 Python 运行器、TCP 事件和评估结果的完整链路。
- 评估已经有公共引擎、方法注册、强类型结果和 ViewModel 投影，适合将性能敏感算法和视觉派生继续收敛。
- 项目级测试使用真实 .dlpro、tests/assets/model 夹具、可配置 Python 环境和 PatchCore 训练/预测/评估链路，能够验证普通单元测试无法覆盖的生命周期。
- CTest、AddTest.cmake、普通测试/项目级测试分组和 Release 构建已经形成可复用基线。

### 2.2 需要结构性处理的问题

| 领域 | 当前风险 | 目标处理 |
| --- | --- | --- |
| 线程 | 导出输入快照已经在工作区建立，但其它后台入口仍可能通过 manager、Qt Model 或项目对象间接取数；评估和聚合的快照边界仍需统一 | GUI 线程生成完整纯值快照，worker 只处理快照；所有入口注册实际句柄 |
| 关闭 | 全局线程池、评估线程池、独立数据线程和外部进程的等待点不统一 | 任务句柄归入 ProjectContext 的关闭栅栏，逐一取消并等待 |
| 回调 | queued 结果缺少统一的 project/task 身份和生命周期判断 | 每个结果携带项目、任务和生命周期代数，终态或关闭后直接丢弃 |
| 数据库 | 批量导入/导出、数据库连接和 Qt Model 刷新边界不清 | 连接归属执行线程，事务留在数据库 Adapter，GUI 只应用结果 |
| Schema | DDL、sqlpp11 类型、运行时 SQL 和任务表可能重复描述 | migration SQL 或等价的唯一 schema 正本，其他内容生成或校验 |
| 模型生命周期 | 文件系统目录、模型数据库和项目记录无法被 SQLite 单事务覆盖 | staging/quarantine、操作记录和恢复状态统一协调 |
| 评估 | 大量 TIFF 重复读取、全像素文本哈希、同步加载全部视觉数据 | 轻量产物身份、预测复用、结果与视觉派生分层、按需生成 |
| 几何 | LabelMe rectangle、Mask 轮廓、模型预处理反映射可能各自处理 | 共享几何模块，输入和显示都使用同一变换 |
| feature | 聚类、搜索和小样本学习依赖完整 manager，失败回滚和任务状态分散 | 只注入最小能力接口，由 feature 协调器编排 |
| 参数/UI | YAML 元数据、框架默认值和 QML 文本可能漂移；运行中 Busy、时间和可编辑状态分散 | YAML 与实际框架代码为事实源，任务状态统一派生 |
| 构建 | imported target、公开头文件和模块依赖可能在构建树/安装树表现不同 | 配置期检查依赖，构建树和安装树都做消费方验证 |

文件行数只是审查信号，不是拆分红线。超过 2000 行或职责混杂时进行结构审查，最终按领域、所有权和变化轴决定是否拆分。

## 3. 目标架构

### 3.1 作用域和所有权

~~~text
ApplicationContext
├── 全局设置与最近项目历史
├── 应用日志和基础设施
└── 应用级 QML 装配

ProjectContext
├── ProjectDataBase
├── DataWorkspace
├── ModelWorkspace
├── FeatureWorkspace
└── ProjectTaskRuntime

TaskScope
├── immutable request / parameter snapshot
├── cancellation token
├── progress channel
├── task state store
└── result / artifact reference
~~~

约束：

- ApplicationContext 不持有项目数据库、项目数据快照或项目任务运行状态。
- ProjectContext 创建、注入、路由和释放项目内对象，并负责关闭顺序。
- 每个耗时操作都有 task_id、取消令牌、进度通道、日志上下文和实际任务句柄。
- 设施型无状态单例可以保留；有状态业务对象必须归属明确的 context。
- TaskManager 可以继续作为 QML 表模型入口，但不能作为业务代码查询全局状态的方式。

### 3.2 领域依赖

~~~text
common / core / database / ui / parameter / settings
                         │
               ┌─────────┴─────────┐
               │                   │
             data                model
               │                   │
               └────────┬──────────┘
                        feature
                          │
                        project
                          │
                         tool
~~~

这是设计上的依赖方向，最终以 CMake target_link_libraries 为准。feature 可以同时依赖 data 和 model；文档不能把真实 DAG 简化成错误的线性顺序。低层不得反向依赖 project、tool 或 QML 页面。

### 3.3 深模块和 Adapter

| 模块 | 对外接口 | 内部职责 |
| --- | --- | --- |
| ProjectContext | 项目初始化、资源所有权、提交操作、关闭 | 数据库、workspace、任务运行时的装配和关闭栅栏 |
| SchemaMigration | 初始化、迁移、校验、当前版本 | SQLite 连接 Adapter、事务和版本状态 |
| DataWorkspace | 数据快照、数据操作、展示投影 | DataManager 外观、数据库读取、Qt Model 和过滤状态 |
| DataTransfer | 导入、导出、进度、取消、结果 | 各格式 DataIO Adapter、批次和最终校验 |
| GeometryKernel | 坐标系、尺寸、Mask、矩形、多边形转换 | 所有格式和评估共享的纯值几何实现 |
| ModelLifecycle | 创建、复制、重命名、删除、恢复 | ModelRecordStore、ModelStorageAdapter 和跨介质协调 |
| TaskRuntime | 状态迁移、协议、取消、结果持久化 | Python 进程、TCP、task.db 和 TaskTableModel |
| EvaluationEngine | 预测读取、指标、阈值和结果 | TIFF Adapter、纯值算法、可取消视觉派生 |
| FeatureWorkspace | 搜索、聚类、智能标注、小样本编排 | 最小数据/模型/任务能力接口 |

深模块的接口应该让调用方不需要知道 SQL、线程池、文件布局和协议细节。只有存在两个可替换生产 Adapter、或测试需要稳定替换点时，才提升内部 seam 为公开接口。

### 3.4 统一数据流

~~~text
QML / Qt Model
      │
      ▼
领域入口生成纯值 Command
      │
      ▼
TaskScope + 后台模块
      │
      ▼
Result + ArtifactReference
      │
      ▼
GUI 线程校验身份并一次性应用可重建投影
~~~

后台输入禁止包含 QObject*、Qt Model、数据库连接和未经所有权约束的 manager 指针。后台结果不得直接修改 QML 状态，必须先通过 project_id、task_id、取消状态和项目生命周期校验。

## 4. 单一事实源和持久化

### 4.1 权威关系

| 数据 | 唯一事实源 | 可重建投影 |
| --- | --- | --- |
| 项目、数据集、图片、标注、类别、Tag、模型身份 | 项目 .dlpro | Qt Model、过滤、统计和缓存 |
| 模型参数、模型级数据集配置 | 模型 model.db | 参数模型和任务准备快照 |
| 单次训练/测试定义、参数、数据集选择、运行状态 | 对应任务目录 task.db | TaskManager 行和项目页摘要 |
| 活跃任务状态迁移 | TaskStateStore | task.db 和 UI 状态 |
| 单次任务原始预测 | 任务 pred/ 文件及任务索引 | 评估结果和视觉派生 |
| 评估指标、图表和实例数据 | 当前 EvaluationResult | QML 图表、混淆矩阵和实例模型 |
| 热力图、多边形和缩略图 | 预测、原图和评估参数的派生缓存 | 进程内有界缓存或临时结果 |

规则：

- TaskStateStore 是唯一接受状态事件并执行状态迁移的模块。
- task.db 是重新打开项目后恢复单次任务定义和运行结果的持久事实源；项目 extra_data 不再保存任务状态、评估应用标记或预测事实。
- 原始 pred/*.tiff 是异常检测像素分数的事实来源，不修改原始 TIFF，不增加第二套评估报告格式。
- 评估结果和视觉缓存丢失后，必须能够从任务数据库、预测产物和项目快照重建。
- 不引入评估 revision；使用任务身份、预测产物身份和参数值判断是否可复用。

### 4.2 Schema 正本和迁移

采用版本化 migration SQL 作为 schema 唯一正本。DDL、sqlpp11 表类型和运行时 SQL 不得继续分别手工维护；需要 C++ 类型时由该正本生成，或通过自动化校验证明其与正本一致。

SchemaMigration 必须提供：

- 空库初始化和当前版本标记；
- 已知版本逐版本迁移；
- 未知版本直接失败；
- 每次迁移在事务中执行，失败后回滚；
- 缺表、字段、类型、约束或索引时给出明确诊断；
- 迁移完成后删除旧建表入口和重复 schema 定义。

数据库接口只暴露领域 Record、批量结果和明确错误。业务模块不拼 SQL，连接在所属执行线程创建和销毁，不能跨线程传递。

### 4.3 强类型领域值

按领域建立单一来源的值类型，例如 DatasetRecord、ImageRecord、LabelRecord、ModelRecord、TaskRecord、ArtifactReference、Result<T> 和 BatchResult<T>。

Record 应表达一组具有关联关系的字段，避免 ids、names、colors、types 等平行数组依靠隐含顺序。Result<T> 统一表达成功、失败、取消、部分结果、错误上下文和可恢复状态。旧的 bool + 输出参数和平行数组在新接口替换完成后删除，不保留双轨。

### 4.4 文件系统与数据库的模型生命周期

SQLite 事务不能覆盖文件系统。模型创建、复制、重命名和删除由 ModelLifecycle 使用可恢复操作记录协调：

~~~text
prepared -> staged -> published -> committed
                    └────────────> needs-recovery
~~~

统一步骤：

1. 校验名称、路径、重复项、权限、进程占用和当前生命周期状态。
2. 在 staging 目录创建或复制完整的模型数据库和必要文件。
3. 校验 staging 的 schema、目录结构和关键产物。
4. 记录 operation_id、操作类型、源、目标、staging/quarantine 路径和状态。
5. 在数据库事务中提交逻辑记录，文件发布使用同卷原子改名或可恢复步骤。
6. 发布完成后标记 committed，再刷新 Qt Model。
7. 任何失败都返回失败或 needs-recovery，不返回与实际介质无关的成功。

具体规则：

- 创建/复制只向 staging 写入，校验后发布最终目录，失败只清理 staging。
- 重命名由同一协调器维护目录名、数据库名和内部引用；回滚失败进入可诊断状态。
- 删除先将目录移入 quarantine，再提交逻辑删除，最后清理 quarantine；清理失败不能报告完全成功。
- 应用启动时恢复未完成操作；重复请求返回已完成、进行中、失败或待恢复，不重复写入。

## 5. 任务、线程和生命周期

### 5.1 线程归属

| 对象/操作 | GUI 线程 | 后台线程 | 约束 |
| --- | --- | --- | --- |
| QML、QObject、Qt Model | 创建、读取、修改 | 禁止访问 | 先生成纯值快照 |
| 项目数据库 | 读取快照、应用结果 | 用线程归属正确的连接执行事务 | 连接不跨线程 |
| 文件系统、外部进程、评估计算 | 提交、展示结果 | 执行 | 输入是绝对路径和纯值 |
| 进度、日志、取消 | 发起、展示 | 发布、检查 | 以任务 ID 关联 |
| 项目关闭 | 阻止新任务、请求取消 | 响应取消并结束 | 等待实际句柄后释放项目 |

必须首先修复的路径：

- 导出开始前由 DataManager 生成包含数据集、图片、标注、类别和目标路径的不可变 DatasetExportSnapshot；DataManagerDatasetExportSource 不再在 worker 中读取 DataManager。
- 评估开始前固化 GlobalFilter、数据集选择、图像尺寸和评估参数；image_dimensions_provider 读取值映射或快照，不捕获 DataManager*。
- 导入批量写库在正确线程的数据库执行上下文中完成，GUI 只接收批次结果并刷新模型。
- ModelTaskController、ModelTestTaskManager、DataOperationWorkflow、聚合任务和外部进程都注册实际任务句柄。

### 5.2 统一状态机

~~~text
created -> preparing -> running -> stopping -> stopped
                         ├──────> succeeded
                         └──────> failed
~~~

约束：

- succeeded、failed、stopped 是唯一终态；不再有暂停态。
- 停止请求先进入 stopping，最终进入 stopped 或明确 failed。
- 终态不可被迟到事件重新打开，重复终态只持久化一次并通知一次。
- 训练、推理、评估、导入、导出和 feature 操作共用状态、取消、进度、日志和关闭契约。
- 参数编辑状态、Busy 状态、运行时间和按钮可用性由同一状态模型派生；任务运行、停止收敛和评估/推理/训练期间全部参数禁用，只保留停止/取消和滚动查看，终态后恢复。

### 5.3 取消、关闭和迟到结果

ProjectContext 关闭时按以下顺序执行：

1. 标记关闭中，拒绝新任务和新的结果发布。
2. 向项目内所有 TaskScope 发出取消。
3. 等待数据线程、评估线程池、任务线程池、外部进程和实际聚合任务句柄。
4. 丢弃尚未应用的结果，确认没有回调仍引用项目对象。
5. 释放数据库、workspace 和 QML 投影。

每个 queued 回调都携带 project_id、task_id、生命周期代数和产物身份。项目关闭、任务进入终态、任务被替换或预测产物变化后，迟到结果直接丢弃，不访问已释放对象。

### 5.4 进度、耗时和日志

- 进度按任务和操作作用域发布；导入/导出等批量操作按批次上报，不能长时间停在 0 后直接跳 100。
- 长任务 info 日志记录开始上下文和结束摘要；结束摘要包含耗时、数量、输出位置、成功/失败和可恢复状态。
- 单个或少量数据集的摘要包含名称，大量数据集只输出数量和汇总。
- 阶段诊断使用可控的 debug 日志和采样，禁止循环中持续写高频日志。
- 运行时间由 C++ 任务生命周期计算，按原有任务上报链路写入 task.db；重新打开项目后从持久化状态恢复展示。
- UI 的 InfoBar 和任务面板只消费同一个耗时字段，不再依赖 Python 偶尔上报才能推进。

## 6. 领域改造

### 6.1 Database

保留 database 作为 SQLite/sqlpp11 的适配层，内部按 schema、连接、事务和领域读写组织实现；不为每一张表机械创建公开 Repository。

接口要求：

- 一次调用表达一个领域意图，而不是暴露 SQL 片段；
- 批量读取返回聚合 Record；
- 批量写入明确原子性、取消、部分成功和错误位置；
- 事务边界位于 database 模块内；
- 可以用同一接口通过 :memory: SQLite 验证行为。

### 6.2 ProjectContext

ProjectContext 是项目聚合和依赖装配的唯一入口，负责：

- 创建和释放 ProjectDataBase、DataWorkspace、ModelWorkspace、FeatureWorkspace 和 ProjectTaskRuntime；
- 给任务注入项目快照、路径和最小能力接口；
- 按关闭栅栏等待所有项目任务；
- 在项目切换时隔离旧项目状态、缓存、任务和回调；
- 通过公开的 Project 接口向 QML 提供项目级投影。

ProjectManager 负责应用级创建、打开、关闭和最近项目历史；不能把项目任务和数据状态存进全局单例。

### 6.3 DataWorkspace 和 DataTransfer

保留 DataManager 的 QML 外观，内部收敛为少数深模块：

1. DataSnapshot：生成数据集、图片、尺寸、标注、类别和路径的纯值快照。
2. DataOperationCoordinator：统一数据集、图片、标注、划分操作的状态、取消、互斥和结果。
3. DataTransfer：统一导入、导出、批次、路径、进度和最终校验。
4. DataPresentationState：维护过滤、统计、Qt Model 刷新和选择等派生状态。

操作完成后只通过一个结果入口更新数据库和展示模型。异常、取消和失败必须释放操作状态，不能依赖多个相互独立的布尔值恢复 UI。

路径和导出规则：

- 本地路径必须是明确的绝对路径；
- UNC 路径识别 \\server\share，不得拼接到应用工作目录；
- 相对路径只在有明确基准目录的接口中解析；
- 目标目录不存在、权限不足、部分写入和最终校验失败都返回失败结果；
- 结束时根据实际输出文件验证导出，不以“曾经写入磁盘”代替成功；
- 导出输出和日志都保留可诊断的目标路径。

### 6.4 DataIO 和 GeometryKernel

保留 DataIO 的多态接口和工厂，物理组织为公共基类、COCO、LabelMe、Mask、Folder 和共享几何实现。格式实现只负责解析、序列化和格式校验；公共基类负责批次、取消、进度和生命周期。

GeometryKernel 统一提供：

- 矩形四点顺序、闭合规则、边界裁剪和退化判断；
- Mask 二值化、尺寸映射、轮廓提取和连通区域；
- 多边形合法性、自交检查和精度规则；
- 模型输入与原图之间的缩放、中心裁剪、padding 和反映射；
- 导入、评估多边形、热力图叠加和 QML 显示共用同一变换。

LabelMe rectangle 只有在确实能得到四个顶点时才转换；轮廓少于三个点才丢弃，不能因为面积小就提前过滤。后续如需筛选，由代理模型或展示过滤层处理原始多边形。

### 6.5 ModelWorkspace

ModelStorageService 只负责统一路径规则和文件操作 Adapter；ModelLifecycle 负责跨介质生命周期；ModelManager 负责 Qt Model 和 QML 适配。

模型创建、复制、重命名、删除必须明确：

- 模型记录、模型目录、model.db 和任务目录的所有权；
- 复制哪些训练参数、数据集配置、任务索引和产物；
- 重命名时所有内部引用和目录名如何同时变更；
- 删除失败、文件占用、权限失败和恢复状态如何呈现。

模型操作不能由 ModelManager 拼装多个半成功步骤。所有操作通过 ModelLifecycle 返回强类型结果，并在结果确认后刷新 QML。

### 6.6 TaskRuntime 和 Python 契约

TaskManager 对外保留 QML 表模型入口，内部建立：

- TaskStateStore：唯一处理事件和合法状态迁移；
- TaskProtocolAdapter：C++ 值对象和 Python 消息的编解码；
- TaskTableModel：role、排序、过滤和展示派生值；
- TaskArtifactStore：任务目录、产物索引和持久化状态。

C++ 与 Python 使用一份契约，覆盖消息类型、必填字段、类型、任务 ID、状态、进度、错误、产物引用和终态。契约可由 schema 生成两侧常量，或由共享校验定义实现，但不能让 C++ 字符串和 Python 字典各自演化。

边界直接拒绝：

- 缺失任务身份；
- 未知状态；
- 非法进度；
- 错误类型不匹配；
- 终态后的事件；
- 无法解析的消息；
- 不属于当前项目或当前任务的结果。

协议错误必须关联任务 ID 和阶段，不静默吞掉。

### 6.7 EvaluationEngine

保留 IEvaluationEngine、方法注册表和 EvaluationResult。将阈值、匹配、图表和视觉派生作为纯值内部 seam，不把算法细节暴露给 ViewModel。

评估数据流：

~~~text
任务参数快照 + 项目数据快照 + pred/*.tiff
  -> 预测产物读取
  -> 指标、匹配和阈值计算
  -> EvaluationResult
  -> 可见实例的按需视觉派生
~~~

评估规则：

- 推理参数改变预测输入或预测文件，启动新的推理；评估参数只作用于已有预测，不重复推理。
- 没有预测产物时不启动评估；评估参数修改只重新评估已有预测。
- 评估过程中的最佳阈值搜索、首次应用、指标计算和结果刷新在同一次评估内完成，不触发第二次评估，也不使用 revision。每个测试任务只在第一次成功评估时自动应用一次最佳阈值；后续评估只展示推荐值，保留用户当前参数。
- 所有预测中的有限、去重分数作为真实候选切分点，精确计算整体 micro-F1、precision、recall 和 PR 曲线；并列时选择较高阈值。该规则同时适用于异常检测、目标检测和语义分割，具体匹配规则由评估方法决定。
- 前端只按现有规则采样约 100 个曲线点，并额外显示最佳阈值点；采样不改变后端最佳值。最佳点 tooltip 显示阈值、F1、精确率和召回率，普通点也显示阈值，数值统一为四位小数。
- 异常检测保留正常/异常两组分数分布。三条线只通过 tooltip 区分为“最佳阈值”“正常最大分数”和“异常最小分数”，不增加额外图例；分布 tooltip 显示“分数”和“数量”。
- 异常检测分类阈值复用于预测结果的像素区域分割；评估参数中的热力图阈值只改变可视化解释，不改变评估指标。
- 目标检测的 max_det、推理阶段 conf/iou 属于 inference；在已有结果上再次过滤的 conf 和用于评估匹配的 iou 属于 evaluation。参数含“阈值”不决定分组，实际是否改变预测文件才决定。

性能规则：

- 图像级评估只保留必要统计和轻量索引，不无条件将全部 TIFF 像素长期物化到内存。
- 不把数亿个浮点像素逐个转文本再拼接 SHA-256；常规失效依据使用任务产物生成状态和轻量文件身份（规范化路径、大小、修改时间、文件 ID 等）。确有完整性要求时只对原始二进制流处理，不能创建文本副本。
- 正常打开评估页面只加载结果、矩阵和必要的实例索引；完整 TIFF、热力图和缩略图按可见实例懒加载。
- 对同一任务、图像、预测产物和视觉参数只生成一次；热力图阈值不变时复用已生成结果，重新推理后整体使旧视觉派生失效。
- 评估和视觉生成使用可取消的后台任务。任务级视觉生成串行化，不能无限触发并行计算，也不能阻塞 GUI。

### 6.8 异常区域、热力图和坐标

原始 pred/<image>.tiff 是唯一像素分数来源，原始文件只读。每个满足几何条件的连通异常区域生成一个多边形，全部叠加显示；提取阶段不按面积过滤，contour 少于三个点时才丢弃。保留原始多边形，后续过滤只通过代理模型或展示投影完成。

显示规则：

- 多边形始终显示，沿用检查页的公共多边形组件和原有红色描边；不增加白色描边或另一套绘制样式；
- 热力图在原图之上、多边形之下，由 QuiToggleSwitch 控制；
- 热力图关闭时显示原图坐标系的图像和多边形；
- 热力图打开时使用模型输入坐标系下的预处理图像、原始分数和多边形，不能把分数图独立缩放到另一坐标系；
- 同一 preprocessing transform 负责模型坐标到原图坐标的反映射，明确处理 resize、center crop 和 padding；
- 缩略图尺寸只影响 QML 渲染，不改变 C++ provider 的坐标和派生数据；
- 派生生成中保留已有可用结果；首次生成尚未完成时使用 BusyIndicator；失败时回退原图，保留多边形和评估数值，不清空评估结果；
- 关闭/打开开关只切换展示，不重复计算；阈值未变不重复生成；重新推理后旧派生失效并按统一生命周期重新生成。

### 6.9 FeatureWorkspace

FeatureManager 保留项目级 QML 聚合入口，搜索、ROI 搜索、图像聚类、ROI 聚类、智能标注和小样本学习分别只依赖最小能力接口：

- 数据快照和查询；
- 过滤和结果写回；
- 模型任务提交；
- 任务状态观察。

聚类目标数据集的创建、任务、文件、数据库写入、网络路径、取消、失败回滚和最终刷新由 feature 协调器统一完成。界面不能同时修改数据集、任务和状态，也不能在 UI 线程执行图像聚类。

### 6.10 参数、配置和 QML

- config/models 是参数元数据、运行时键、分组、默认值和中文说明的唯一来源。
- name_en 是持久化和运行时键；name_cn 是准确的界面名称，必须根据 EasyTrain、Anomalib、Ultralytics 的实际代码含义审查，不能从字段名猜测。
- 默认值优先采用框架实际提供的合理默认值；项目特定值明确写在 YAML；不增加 auto 隐含状态。
- 数据加载进程默认使用 2，最大允许值按实际框架和机器约束设置为 128；batch 默认 8 保持不变。
- 测试参数只有 inference 和 evaluation 两组，不再建立 data 等重复组。FS-SAM2 等内部使用能力不加入普通测试评估参数。
- inference 修改只改变下一次推理输入；evaluation 修改只重新解释已有预测。旧任务没有新组时不建立兼容分支，按新契约直接拒绝无法解析的输入。
- QML 只负责布局、绑定、轻量交互和结果呈现，不负责数据库事务、协议解析、坐标反映射或评估算法。
- 参数、Busy、运行时间、停止按钮和 InfoBar 都绑定同一任务状态模型。训练、推理、评估和停止收敛时全部禁用编辑，只保留停止/取消和滚动查看。
- 使用现有 QuiComboBox、QuiToggleSwitch、检查页多边形组件和 ModelFormDialog.qml 的弹窗风格，避免页面重新实现一套控件状态。
- UI 图表使用紧凑的互斥控件或下拉切换，不用大尺寸 tab 占用图表区域；图表 tooltip 只显示用户需要的中文字段。

## 7. 构建、依赖和可观测性

### 7.1 CMake

- 以 target_link_libraries 维护真实依赖 DAG，配置阶段发现反向依赖和不存在的 target。
- 公开头文件只包含调用者真正需要的类型；实现类型前向声明或移入 .cpp。
- AddTest.cmake 统一普通测试、QML 测试和项目级测试的注册、环境、标签、fixture 和工作目录；测试源使用 GLOB CONFIGURE_DEPENDS。
- 测试可执行文件只通过 CTest 启动，Python 工具只负责构建、环境和筛选。
- find_package 使用 imported target 前必须确保依赖包已提供该 target。InferRT、ONNX Runtime、FAISS 等缺少目标时在配置期给出明确诊断，不在下游伪造空 target。
- 同时验证构建树和安装树，确保头文件、库、DLL、QML 模块、配置文件和 imported target 都能被消费方找到。

### 7.2 环境、路径和发布

- tools/dependencies.yaml 不把个人开发机绝对路径当成跨机器默认事实；路径通过命令行、环境变量或本地未跟踪覆盖注入。
- Python 环境由项目级测试脚本选择，测试、构建和产品运行环境不互相写死。
- CUDA、InferRT、OpenCV 和模型框架等依赖在 configure 阶段报告是否启用、缺失原因和配置路径。
- 发布脚本统一检查版本、依赖、Qt/QML 运行时、DLL、重解析点和安装树可运行性。

### 7.3 日志和诊断

结构化日志至少关联 project_id、task_id、阶段、输入规模、输出规模、耗时和状态。长任务只在阶段边界和结束输出摘要，高频进度通过进度通道发布。

评估诊断保留阶段级总耗时和规模信息，例如图像读取、预测读取、指标计算、视觉派生、结果发布和 UI 聚合；问题定位结束后删除或关闭高频 debug 打印。日志不能在每张图、每个像素或每个 UI 重建循环中写入。

## 8. 分阶段实施路线

每阶段都必须保持可编译、可运行；先补行为测试，再替换实现；每次只跨一个真实 seam；旧路径在新路径稳定后删除。

### 阶段 0：事实源、构建和回归基线

目标：建立可重复的构建、测试选择和文档事实源。

工作：

- 审查 CMake target DAG、公开头依赖、安装导出和 imported target。
- 盘点 DDL、sqlpp11、运行时 SQL、任务表和 extra_data 投影。
- 盘点后台入口、线程池、独立线程、取消入口、回调连接和项目关闭等待点。
- 固化普通测试/项目级测试边界、AddTest.cmake、CTest 标签、Python 脚本和真实夹具。

出口：

- Release 可配置和构建；
- 普通测试、Model/QML 测试和 tools 测试可由 CTest 选择；
- 项目级 full 与单层测试的前置条件语义明确；
- 无关的用户环境配置不进入阶段提交。

当前仓库已具备这一阶段的构建拓扑、AddTest.cmake 和模型测试选择基线；继续实施前以实际 CMake/CTest 结果复核。

### 阶段 1：线程边界和快照

目标：消除 worker 回读 GUI/project 对象。

工作：

- 先为 DataManagerDatasetExportSource 增加快照行为测试，再实现 DatasetExportSnapshot。
- 评估开始时生成数据集选择、图像尺寸、过滤和参数快照，移除 image_dimensions_provider 对 DataManager 的捕获。
- 为导入、导出、评估和聚合结果增加 project/task 身份校验。
- 为所有后台入口返回并注册实际任务句柄。

出口：后台任务只消费纯值；快照内容变化可被测试观察；GUI 对象不跨线程访问。

### 阶段 2：Schema 和强类型数据库接口

目标：消除 schema 与弱类型数据库接口分叉。

工作：

- 选择版本化 migration SQL 或唯一可生成 schema 作为正本，移除重复建表入口。
- 实现未知版本拒绝、逐级迁移、事务回滚和结构校验。
- 建立 Dataset/Image/Label/Model/Task Record、Result<T> 和 BatchResult<T>。
- 用 :memory: 测试数据库连接、事务、批量写入和线程归属。

出口：业务层不拼 SQL；空库、迁移、未知版本、结构错误和回滚都有行为测试。

### 阶段 3：ProjectContext、TaskRuntime 和关闭栅栏

目标：统一项目作用域、任务状态和关闭时序。

工作：

- 引入显式 ApplicationContext/ProjectContext 装配，清除跨项目全局任务状态。
- 建立 TaskStateStore、TaskProtocolAdapter、TaskArtifactStore 和任务表投影。
- 固化 created/preparing/running/stopping/stopped/succeeded/failed 状态机。
- 统一取消、进度、日志、运行时间持久化和外部进程结束语义。
- 项目关闭时取消并等待所有实际任务句柄，丢弃迟到结果。

出口：两个项目互不污染任务和缓存；重复终态、进程崩溃、停止、关闭和迟到消息都有测试。

### 阶段 4：DataWorkspace、DataTransfer 和几何内核

目标：让数据操作可快照、可取消、可回滚，格式和评估使用同一坐标规则。

工作：

- 在 DataManager 内部建立快照、操作协调、传输和展示状态 seam。
- 按格式物理拆分 DataIO，保留已有多态接口和工厂。
- 抽取 GeometryKernel，覆盖 rectangle、Mask、polygon、resize、crop、padding 和反映射。
- 修复本地/UNC/相对路径、导出最终校验、批次进度和导入写库线程归属。
- 增加损坏输入、单像素区域、LabelMe rectangle 和尺寸差异测试。

出口：导入、导出、取消、进度、格式错误、路径错误和坐标映射可独立验证。

### 阶段 5：ModelWorkspace 和跨介质生命周期

目标：让模型记录、model.db、模型目录和任务目录一致且可恢复。

工作：

- 实现 ModelLifecycle、ModelRecordStore 和 ModelStorageAdapter。
- 建立 staging/quarantine 和操作记录。
- 收敛创建、复制、重命名、删除的事务/文件协调。
- 固定复制范围和内存/持久化语义。
- 删除 project extra_data 中不可重建的任务状态投影。

出口：目标已存在、数据库失败、权限失败、文件占用、复制中断、重复操作和重启恢复都有测试。

### 阶段 6：EvaluationEngine 和视觉派生

目标：复用预测产物，降低评估和实例图像的等待时间。

工作：

- 评估输入完全快照化，删除 worker 对 GUI 对象的间接读取。
- 将阈值候选、PR/micro-F1、匹配、图表采样、异常区域和热力图做成可测试的纯值 seam。
- 使用预测文件轻量身份和产物状态失效缓存，移除像素转文本哈希。
- 分离预测读取、评估结果、视觉派生和 UI 实例懒加载。
- 统一分类阈值分割、模型坐标/原图坐标、多边形、热力图、失败回退和重新推理失效。
- 增加大 TIFF、取消、重复打开、阈值修改、预测替换和生成失败测试。

出口：评估参数不重复推理；没有预测不评估；同一输入不重复生成；实例按需加载；UI 不被 worker 阻塞。

### 阶段 7：Feature、配置和 QML 投影

目标：收窄 feature 依赖，消除配置和页面状态分叉。

工作：

- 为搜索、聚类、智能标注和小样本任务定义最小数据/模型/任务接口。
- FeatureManager 只负责项目装配和 QML 入口。
- 统一 YAML 参数元数据、框架默认值、name_en、name_cn 和中文简介的审查流程。
- 统一 inference/evaluation 分组和运行中全部参数禁用语义。
- 统一 Busy、进度、耗时、停止、失败恢复、图表下拉切换和弹窗样式。
- 删除已无调用的旧路径、重复字段和状态投影。

出口：feature 可用最小 Adapter 测试；配置只改一处；QML 不包含业务算法和持久化逻辑。

### 阶段 8：构建、安装和持续验收

目标：在干净、可注入的环境中稳定配置、构建和执行。

工作：

- 修复可选 imported target、安装导出、DLL/QML 运行时和 CMake 诊断。
- 移除开发机路径默认值，统一命令行/环境覆盖。
- 建立 ordinary、data、model、qml、project 和 pressure 测试矩阵。
- 增加损坏图片/TIFF/数据库、权限、网络共享、大数据量和取消收敛夹具。
- 更新 docs/ 导航，只保留稳定索引并指向源码事实源。

出口：构建树、安装树、CTest、普通测试、Model/QML 测试和项目级 full 流程可重复执行。

## 9. 测试体系和验收矩阵

### 9.1 测试分层

| 层级 | 关注内容 | 资源和执行 |
| --- | --- | --- |
| 纯值测试 | 几何、阈值、PR/micro-F1、匹配、状态迁移、协议编解码 | 内存值对象，通过 CTest |
| 数据库测试 | schema、迁移、事务、Record、批量写入 | :memory: 或临时 SQLite，通过 CTest |
| 领域模块测试 | ModelLifecycle、DataTransfer、TaskRuntime、Feature | 临时文件系统和内存 Adapter |
| C++/QML 测试 | ViewModel、注册表、状态、懒加载、失败回退 | CTest 注入 Qt/QML/DLL |
| 普通集成测试 | data/model/UI 真实模块组合 | 构建资源和可控临时目录 |
| 项目级测试 | .dlpro、数据导入导出、模型增删改拷、训练/预测/评估 | tests/assets、可配置 Python、CTest fixture |
| 压力测试 | 大文件列表、TIFF、取消、网络路径、迟到事件 | 明确规模、可取消、失败注入 |

普通测试不依赖项目级 .dlpro、用户模型权重或本机 Python 环境。项目级测试只在 full 模式负责从空根目录按 fixture 完成全链路；非 full 层默认复用已有产物，不隐式补齐前置条件，缺少前置产物必须由当前层失败。所有测试由 CTest 启动，不能直接运行测试可执行文件。

### 9.2 必须验证的不变量

- 失败操作不会返回与数据库或磁盘状态不符的完全成功。
- schema 只从唯一正本生成；未知版本拒绝；迁移失败回滚。
- 后台不访问 GUI 对象、Qt Model、跨线程数据库连接或裸 manager 指针。
- 项目之间不共享任务、进度、评估缓存和数据库连接。
- 任务终态不可被重复事件和迟到消息改变。
- 项目关闭后没有能发布到旧项目对象的后台回调。
- UNC 路径不会被拼接到应用工作目录；导出可从实际目录验证。
- 推理参数改变才启动推理；评估参数只在已有预测上重新评估。
- 原始 TIFF 不被修改；热力图和多边形可以失效、取消和重建。
- 相同预测与视觉参数不重复生成；懒加载不阻塞 UI。
- 异常检测保持正常/异常两组分数分布，最佳阈值和指标来源一致。
- 评估使用精确候选分数求解全局 micro-F1；前端采样只影响展示。
- Model 测试脚本和 CTest 标签不会遗漏评估和行为测试。
- 项目级空项目、复用项目、显式重建和 full 模式的前置条件互不混淆。

### 9.3 验收顺序

具体构建目录、配置、Python 环境、项目名称、数据集名称和 CTest 名称以 docs/TESTING.md、CMake 和脚本事实源为准。每一阶段至少完成：

1. 对应纯值/领域行为测试；
2. CMake configure；
3. Release build；
4. 相关 CTest 目标和标签；
5. 需要真实资源时执行项目级分层测试；
6. 记录成功、失败、取消、耗时和未验证项。

整体验收覆盖：

~~~text
configure
  -> Release build
  -> ordinary CTest
  -> model/QML CTest
  -> project-layer 单层验证
  -> project-layer full
  -> 安装树消费方和桌面烟测
~~~

性能验收记录阶段耗时、输入规模、内存峰值、缓存命中、UI 线程响应和取消收敛时间，不设置机械的代码行数或数据量红线。

## 10. 迁移和提交纪律

- 先写能观察行为的测试，再移动实现；测试穿过深模块接口，不绑定内部文件布局。
- 每次只跨一个真实 seam，重构后删除旧路径和重复实现。
- 存量数据库如需变换，执行一次性迁移；完成后删除旧入口，不增加隐式兼容分支。
- QVariantMap 只允许存在于 JSON、图表和 QML 动态边界，进入领域层立即转换为强类型值。
- 不为每张表、每个页面或每个函数增加只有一层转发的 facade。
- 不把数据库、任务协议、数据格式和项目生命周期搬进 common；只有真正跨模块复用的基础设施才进入 common。
- 不用加锁、同步回调、缓存字段或高频日志掩盖对象归属和事实源问题。
- 每个阶段的提交只包含本阶段代码、测试和必要索引文档，不混入已有用户改动。
- 每个阶段结束后按仓库 `WORKLOG.md` 规则记录目标、状态、验证证据和下一步；仅维护本方案或其它 `docs` 文档时不额外制造进度条目。

建议的逻辑提交顺序：

1. 收敛构建拓扑、公开头和测试选择；
2. 导出/评估输入快照化；
3. 统一任务句柄、取消、项目关闭和迟到回调；
4. 统一 schema、Record 和数据库事务；
5. 重构 DataWorkspace、DataTransfer 和 GeometryKernel；
6. 收敛 ModelLifecycle 和跨介质持久化；
7. 收敛 TaskRuntime、Python 契约和参数；
8. 优化 EvaluationEngine、阈值搜索和视觉懒加载；
9. 收窄 FeatureWorkspace、QML 投影和构建发布；
10. 删除旧路径并完成全量验收。

每个逻辑提交都应可构建、可测试、可恢复，不以一个大提交隐藏多个未验证的结构变化。

## 11. 明确不做

- 不把桌面应用拆成网络服务、事件总线或大量只转发调用的浅模块。
- 不为每张数据库表机械创建公开 Repository。
- 不保留旧任务参数组、旧评估 revision、旧 SQL 入口或兼容双轨。
- 不增加热力图/多边形的第二套持久化格式，不修改原始 pred/*.tiff。
- 不用全量浮点文本序列化、无界内存缓存或高频日志制造完整性假象。
- 不让 QML 负责数据库事务、协议解析、坐标反映射、训练/推理/评估计算。
- 不修改 Anomalib 内部实现来迁就 UI；一致性问题在评估输入和展示派生层解决。
- 不把 2000 行、类数量、目录数量或测试数量当成架构质量本身。
- 不为了“兼容旧任务”保留已经明确废弃的字段、分组或状态。

## 12. 最终完成定义

只有以下条件全部满足，整体改进才算完成：

1. 领域依赖 DAG、数据库 schema、任务协议、参数 YAML、预测产物和测试注册各自只有一个事实源。
2. 所有耗时操作都有纯值输入、实际任务句柄、取消令牌、进度和生命周期校验。
3. 项目关闭和任务停止能够等待所有执行者收敛，迟到回调不会访问旧对象。
4. 数据库事务、文件系统操作和模型生命周期的成功/失败/待恢复语义一致且可测试。
5. DataManager、DataIO、TaskManager 和 FeatureManager 的拆分均对应真实职责或测试 seam，没有浅层包装。
6. C++/Python 协议、状态、运行时间和任务产物在重新打开项目后可恢复。
7. 评估复用已有预测；阈值搜索、micro-F1、PR、分布图、热力图、多边形和缩略图具有明确的坐标、懒加载、缓存失效和失败回退规则。
8. 普通测试、Model/QML 测试、项目级分层测试和 full 流程全部由 CTest 可重复执行，缺少前置条件不会被 fixture 静默补齐。
9. Release 构建、安装树和可配置的 Python/InferRT/Qt 环境可验证，缺失依赖给出明确诊断。
10. docs/ 只承担稳定索引和架构约定，代码、CMake、DDL、YAML 和 CTest 继续作为唯一事实源。

完成定义不以某个固定耗时或文件行数作为替代指标；必须用行为测试、真实资源流程、失败注入和可观测性证据证明架构已经收敛。
