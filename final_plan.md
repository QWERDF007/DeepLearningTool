# DeepLearningTool 最终架构改进方案

## 0. 文档定位

本文是 DeepLearningTool 后续架构改造的唯一方案入口，负责记录目标架构、职责划分、迁移顺序、验收标准和明确不做的事项。它不复制运行时细节，也不替代源码事实源。

运行时事实按以下位置维护：

| 内容 | 唯一事实源 |
| --- | --- |
| 模块、构建选项、安装导出和依赖 | 根目录 `CMakeLists.txt`、`src/CMakeLists.txt`、各模块 `CMakeLists.txt` |
| C++ 接口、所有权、线程归属和状态实现 | `src/**/include/`、对应实现文件 |
| 数据库结构、字段、约束和迁移 | `src/database/include/database/ddl/` 及数据库迁移实现 |
| 模型参数、分组、默认值和说明 | `config/models/` 以及实际使用的 EasyTrain、Anomalib、Ultralytics 代码 |
| Python 任务协议 | `src/model/` 与 `3rdparty/EasyTrain/` 的协议实现 |
| 测试注册、标签、fixture 和执行方式 | `tests/`、`cmake/AddTest.cmake`、`tools/run_model_tests.py`、`tools/run_project_tests.py` |
| 稳定的项目约定和导航 | [`docs/README.md`](docs/README.md) 及其索引文档 |

当本文与源码、CMake、DDL、YAML、任务协议或 CTest 注册不一致时，以运行时事实源为准，并只更新本文的索引、决策和实施状态，不在文档中复制第二套字段定义。

### 0.1 统一裁决

后续设计和实现统一按以下顺序裁决：

1. 先保证所有权、项目隔离、任务终态和数据一致性，再优化吞吐量和界面响应。
2. 先固定事实源、输入快照和结果身份，再引入缓存、异步派生和懒加载。
3. 只在真实职责、变化轴、所有权或测试 seam 存在时拆分模块；模块应以小接口承载深实现，不以转发类制造形式分层。
4. 跨线程只传不可变值和明确的任务句柄；worker 不回读 manager、Qt Model、QObject 或跨线程数据库连接。
5. 文件系统与 SQLite 的协调必须显式表达提交、失败、取消和待恢复；任何部分成功都不能伪装为完全成功。
6. 迁移按可独立验证的垂直切片推进：先写行为测试，再替换实现，稳定后删除旧路径；每个切片都必须保持 Release 可构建并能由 CTest 验证。
7. 性能优化以阶段耗时、内存峰值、取消收敛和 UI 响应证据为依据，不以固定行数、类数量或一次性“大重构”作为完成标准。

## 1. 目标与约束

### 1.1 总目标

在保留 Qt 6/QML、C++、SQLite、CMake 和 Python/EasyTrain 技术主干的前提下，收敛以下问题：

- 项目对象、全局对象和后台任务的所有权不清；
- worker 间接访问 Qt Model、DataManager、项目对象或跨线程数据库连接；
- 取消、停止、项目关闭和迟到回调不能稳定收敛；
- 文件系统状态和 SQLite 记录出现半成功；
- schema、参数、任务协议和展示状态存在多个事实源；
- 评估重复读取预测文件、重复生成视觉结果并阻塞 UI；
- 导入、评估和 QML 展示分别实现坐标转换，导致偏移；
- QML 页面承载业务规则、协议和持久化逻辑；
- 构建树、安装树和外部依赖的诊断不一致。

### 1.2 不可破坏的原则

1. **单一事实源**：定义、持久状态、原始产物和展示数据分别只有一个权威来源，其他数据必须可重建。
2. **深模块**：复杂行为集中在少数有深度的模块中，调用方只依赖小接口；接口同时表达错误、顺序、取消和性能契约。
3. **纯值跨线程**：worker 只接受不可变值、绝对路径、取消令牌和明确的 Adapter，不捕获 QObject、Qt Model、数据库连接或裸 manager 指针。
4. **按真实职责拆分**：只有职责、变化轴、所有权或可测试 seam 真实存在时才拆模块；不按行数机械拆分。
5. **破坏性重构**：明确废弃的任务、参数组、revision、SQL 入口和兼容双轨直接删除；新契约不成立时直接报错。
6. **先测后改**：先增加穿过目标接口的行为测试，再替换实现；未验证的结果不能写成完成。
7. **可恢复状态**：跨文件系统和数据库的操作必须显式表达成功、失败、取消和待恢复，不把部分写入报告为成功。

### 1.3 优先级

| 优先级 | 问题 | 完成条件 |
| --- | --- | --- |
| P0 | 越界访问、项目关闭、任务终态 | 所有后台操作可取消、可等待；关闭后不再访问旧项目；迟到事件不可改变终态 |
| P0 | 文件与数据库不一致 | 创建、复制、重命名、删除、导入和导出都有明确的提交、回滚和待恢复语义 |
| P0 | 多份 schema、任务状态和预测事实 | 每类事实只有一个权威来源，未知版本和非法事件直接失败 |
| P1 | DataManager、DataIO、任务编排职责混杂 | 通过少数深模块和内部 seam 收敛职责，不产生浅层转发类 |
| P1 | 评估重复计算和内存膨胀 | 预测复用、视觉派生懒加载、缓存有界且可失效，UI 线程不执行重计算 |
| P1 | 坐标转换重复实现 | 一个几何内核统一导入、评估和 QML 显示的缩放、裁剪、padding 和反映射 |
| P2 | 配置、CMake、日志和测试分叉 | 配置可注入，构建可诊断，日志有界，CTest 分层可重复执行 |

## 2. 现状判断

### 2.1 应保留的有效结构

- `Project` 已经是项目级聚合对象，适合作为项目资源和生命周期的所有权根。
- `data` 已有 DataIO 多态扩展点，COCO、LabelMe、Mask、Folder 可以在此基础上收敛公共规则。
- `model` 已具备模型记录、任务准备、外部 Python 运行器、任务事件和评估引擎链路。
- `IEvaluationEngine`、评估方法注册表、`EvaluationResult` 和 ViewModel 已形成可测试的评估 seam。
- CMake、`AddTest.cmake`、CTest 标签、普通测试/项目级测试分离以及项目级分层脚本已经形成执行基线。
- 真实 `.dlpro`、`tests/assets/model`、可配置 Python 环境和 PatchCore 项目级测试能覆盖普通测试无法覆盖的流程。

### 2.2 必须结构性处理的风险

| 领域 | 风险 | 处理方向 |
| --- | --- | --- |
| 作用域 | 全局任务、缓存或数据库状态可能跨项目复用 | `ApplicationContext` 只持有应用资源，项目状态归 `ProjectContext` |
| 线程 | 后台任务通过 manager 或 Qt Model 回读最新状态 | GUI 线程生成完整快照，worker 只消费快照 |
| 生命周期 | 线程池、外部进程、评估任务、数据操作和聚合任务关闭顺序不同 | 所有实际执行者注册到项目关闭栅栏 |
| 回调 | queued 回调没有统一身份和生命周期判断 | 结果携带 project/task/generation/artifact 身份，迟到结果直接丢弃 |
| 数据库 | 批量操作的事务、连接和模型刷新职责重叠 | 连接归执行线程，事务留在 database，GUI 只应用结果 |
| 文件操作 | SQLite 事务不能覆盖模型目录和预测产物 | staging/quarantine 加操作记录，发布后再提交逻辑记录 |
| 评估 | TIFF 读取、阈值搜索、热力图和实例加载互相耦合 | 预测读取、评估结果和视觉派生分层并懒加载 |
| 几何 | rectangle、Mask、多边形和模型预处理分别转换 | `GeometryKernel` 统一变换和退化规则 |
| 参数/UI | YAML、框架默认值和 QML 文本可能漂移 | 参数元数据与实际框架代码联合审查，QML 只做投影 |

文件行数、类数量和目录数量只是审查信号，不是拆分条件。拆分必须消除真实职责、变化轴或测试成本。

## 3. 目标架构

### 3.1 作用域和所有权

```text
ApplicationContext
├── 全局设置、最近项目和应用基础设施
└── 应用级 QML 装配

ProjectContext
├── ProjectDataBase
├── DataWorkspace
├── ModelWorkspace
├── FeatureWorkspace
└── ProjectTaskRuntime

TaskScope
├── immutable request snapshot
├── cancellation token
├── progress and log context
├── actual execution handle
└── result/artifact reference
```

约束：

- `ApplicationContext` 不持有项目数据库、项目数据快照或项目任务状态。
- `ProjectContext` 创建、注入、路由和释放项目内资源，并负责关闭栅栏。
- 有状态业务对象必须有明确的 context 所有权；无状态设施才可作为应用级对象。
- `TaskManager` 可以继续作为 QML 表模型入口，但业务模块不能以全局单例查询项目任务。
- 项目切换必须隔离数据库连接、任务、缓存、回调和 QML 投影。

### 3.2 依赖规则

实际依赖以 CMake target 为准，目标规则如下：

```text
common / core / database / ui / parameter / settings
                         │
               ┌─────────┴─────────┐
               │                   │
             data                model
               │                   │
               └────────┬──────────┘
                        feature
                          │
                        project
                          │
                         tool
```

- 低层不依赖 `project`、`tool` 或 QML 页面。
- `database` 只提供持久化适配，不承载领域编排。
- `data` 和 `model` 不互相通过全局对象取状态；跨领域需求通过纯值或最小能力接口传递。
- `feature` 可以组合 data 和 model，但不反向修改它们的内部实现。
- 任何新增依赖都必须在 CMake target 层表达，并有对应测试。

### 3.3 深模块

| 模块 | 小接口 | 封装的复杂度 |
| --- | --- | --- |
| `ProjectContext` | 打开、提交、关闭和资源访问 | 项目资源装配、关闭顺序和生命周期隔离 |
| `SchemaMigration` | 初始化、迁移、校验和当前版本 | SQLite 连接、事务、版本和结构诊断 |
| `DataWorkspace` | 数据快照、数据操作和展示投影 | DataManager 外观、数据库读取、过滤和模型刷新 |
| `DataTransfer` | 导入、导出、进度、取消和结果 | 格式 Adapter、批次、路径校验、回滚和产物校验 |
| `GeometryKernel` | 几何值转换和合法性判断 | rectangle、Mask、polygon、resize、crop、padding 和反映射 |
| `ModelLifecycle` | 创建、复制、重命名、删除和恢复 | model.db、模型目录、任务目录与数据库记录协调 |
| `TaskRuntime` | 提交、停止、状态、结果和关闭 | Python 进程、协议、任务存储、进度和终态 |
| `EvaluationEngine` | 提交评估、取消和消费结果 | 预测读取、匹配、指标、阈值、图表和视觉派生 |
| `FeatureWorkspace` | 搜索、聚类、智能标注和小样本编排 | 最小数据/模型/任务能力和失败回滚 |

只有存在两个可替换的生产 Adapter，或测试需要稳定替换点时，才将内部 seam 提升为公开接口。

### 3.4 统一数据流

```text
QML / Qt Model
      │
      ▼
领域入口生成纯值 Command
      │
      ▼
TaskScope + 后台模块
      │
      ▼
Result + ArtifactReference
      │
      ▼
GUI 线程校验身份并一次性应用可重建投影
```

后台输入禁止包含 QObject、Qt Model、数据库连接和未经所有权约束的 manager 指针。后台结果必须经过项目、任务、生命周期和产物身份校验后才能发布。

## 4. 单一事实源与持久化

### 4.1 权威关系

| 数据 | 权威来源 | 派生投影 |
| --- | --- | --- |
| 项目、数据集、图片、标注、类别、Tag、模型身份 | 项目 `.dlpro` | Qt Model、过滤、统计和缓存 |
| 模型参数和模型级数据集配置 | 模型 `model.db` | 参数对象和任务准备快照 |
| 单次训练/推理/评估定义、参数和运行状态 | 任务目录 `task.db` | TaskManager 行和页面摘要 |
| 任务状态迁移 | `TaskStateStore` | `task.db` 和 UI 状态 |
| 原始预测 | 任务目录 `pred/` 及任务索引 | 评估结果和视觉派生 |
| 评估指标、矩阵、图表和实例索引 | 当前 `EvaluationResult` | QML 图表、矩阵和实例列表 |
| 热力图、多边形和缩略图 | 原图、预测和评估参数 | 有界进程缓存或临时派生结果 |

规则：

- `TaskStateStore` 是唯一接受任务事件并执行状态迁移的模块。
- 项目 `extra_data` 不保存不可重建的任务状态、评估应用标记或预测事实。
- 异常检测原始 `pred/*.tiff` 只读，是像素分数唯一来源，不修改文件，也不增加第二套评估报告格式。
- 评估结果和视觉缓存丢失后，必须能从任务数据库、预测产物和项目数据重建。
- 不引入评估 revision；复用由任务身份、预测产物身份和实际参数值决定。

### 4.2 Schema 和数据库接口

采用版本化 migration SQL 或等价的唯一 schema 正本。运行时建表 SQL、DDL、sqlpp11 类型和手工字段列表不能分别演化。

`SchemaMigration` 必须支持：

- 空库初始化和当前版本标记；
- 已知版本逐级迁移；
- 未知版本直接失败；
- 每次迁移在事务内执行，失败回滚；
- 缺表、字段、约束和索引的明确诊断；
- 迁移完成后删除旧建表入口和重复 schema 定义。

database 接口只返回领域 Record、批量结果和明确错误。业务模块不拼 SQL；数据库连接在所属执行线程创建和释放，不能跨线程传递。

### 4.3 强类型值对象

按领域建立单一来源的 `DatasetRecord`、`ImageRecord`、`LabelRecord`、`ModelRecord`、`TaskRecord`、`ArtifactReference`、`Result<T>` 和 `BatchResult<T>`。具有关联关系的字段使用 Record 表达，不使用依赖隐含顺序的平行数组。

`Result<T>` 必须区分成功、失败、取消、部分结果和待恢复，并带有错误上下文。新接口替换完成后删除旧的 `bool + 输出参数` 和重复 `QVariantMap` 双轨。

### 4.4 文件系统与数据库协调

SQLite 事务不能覆盖文件系统。模型和任务产物使用可恢复操作状态：

```text
prepared -> staged -> published -> committed
                    └────────────> needs-recovery
```

统一步骤：

1. 校验名称、路径、重复项、权限、占用状态和当前生命周期。
2. 在 staging 目录创建或复制完整内容。
3. 校验 staging 的 schema、目录结构和关键产物。
4. 记录操作 ID、类型、源、目标、staging/quarantine 路径和状态。
5. 在数据库事务中提交逻辑记录，使用同卷原子改名或可恢复步骤发布文件。
6. 发布确认后标记 committed，再刷新 Qt Model。
7. 失败、取消或清理失败必须返回对应状态，不能伪装成完全成功。

删除先移动到 quarantine，再提交逻辑删除，最后清理 quarantine；应用启动时恢复未完成操作。重复请求必须返回已完成、进行中、失败或待恢复，不重复写入。

## 5. 任务、线程与生命周期

### 5.1 任务句柄

所有导入、导出、训练、推理、评估、聚合、搜索、聚类和小样本操作都必须生成 `TaskScope`，包含：

- 唯一 `task_id` 和项目身份；
- 不可变请求/参数快照；
- 取消令牌；
- 实际执行句柄；
- 进度和日志上下文；
- 结果及产物引用；
- 生命周期代数或等价的结果发布凭证。

GUI 线程负责创建快照和提交任务；后台线程负责 I/O、外部进程和计算；结果回到 GUI 线程后只应用通过身份校验的值。

### 5.2 统一状态机

```text
created -> preparing -> running -> stopping -> stopped
                         ├──────> succeeded
                         └──────> failed
```

- `succeeded`、`failed`、`stopped` 是唯一终态，不保留暂停态。
- 停止请求先进入 `stopping`，执行者真正结束后进入 `stopped` 或明确 `failed`。
- 终态不可被迟到事件重新打开；重复终态只持久化一次并通知一次。
- 运行、停止收敛和评估/推理/训练期间禁用全部参数和操作编辑，只保留停止/取消和滚动查看；终态后恢复。
- 运行时间由 C++ 任务生命周期计算，并沿现有写入链路持久化到 `task.db`；重新打开项目后从持久化状态恢复。

### 5.3 取消与项目关闭

`ProjectContext` 关闭按固定顺序执行：

1. 标记关闭中，拒绝新任务和新的结果发布。
2. 向所有 `TaskScope` 发出取消。
3. 等待数据线程、评估线程池、任务线程池、外部进程和聚合句柄。
4. 丢弃尚未应用的结果，确认没有回调仍引用项目对象。
5. 释放数据库、workspace 和 QML 投影。

每个 queued 回调携带项目、任务、生命周期代数和产物身份。项目关闭、任务终态、任务替换或预测产物变化后，迟到结果直接丢弃，不访问旧对象。

### 5.4 进度、耗时和日志

- 批量导入和导出按批次发布进度，不能长时间停在 0 后直接跳到 100。
- 长任务结束日志以 `info` 级别记录耗时、数量、输出位置、状态和可恢复状态；少量数据集包含名称，大量数据集只输出数量和汇总。
- 阶段诊断使用可控的 debug 日志和采样，禁止在每张图、每个像素或每个 UI 重建循环中写日志。
- InfoBar、任务面板和 QML 运行时间都消费同一个持久化耗时字段，不依赖 Python 是否及时上报。

## 6. 领域改造方案

### 6.1 Database

保留 database 作为 SQLite/sqlpp11 适配层，内部按 schema、连接、事务和领域读写组织实现，不为每张表机械创建公开 Repository。

接口要求：

- 一次调用表达领域意图，而不是暴露 SQL 片段；
- 批量读取返回聚合 Record；
- 批量写入明确原子性、取消、部分成功和错误位置；
- 事务边界留在 database；
- 使用 `:memory:` 或临时 SQLite 验证行为和回滚。

### 6.2 ProjectContext

`ProjectContext` 是项目聚合和依赖装配入口，负责创建和释放项目数据库、DataWorkspace、ModelWorkspace、FeatureWorkspace 和 ProjectTaskRuntime，向任务注入项目快照和最小能力接口，并执行关闭栅栏。

`ProjectManager` 只负责应用级创建、打开、关闭和最近项目历史，不能把项目任务和数据状态存进全局单例。

### 6.3 DataWorkspace 与 DataTransfer

保留 DataManager 的 QML 外观，内部按真实职责收敛为：

1. `DataSnapshot`：生成数据集、图片、尺寸、标注、类别和路径的纯值快照；
2. `DataOperationCoordinator`：统一数据集、图片、标注和划分操作的互斥、取消、状态和结果；
3. `DataTransfer`：统一导入、导出、批次、路径、进度、回滚和最终校验；
4. `DataPresentationState`：维护过滤、统计、Qt Model 刷新和选择等派生状态。

操作完成后只通过一个结果入口更新数据库和展示模型。导入批次失败或取消时，新增图片、标注、类别以及被修改的既有类别属性必须整体回滚；GUI 不在 worker 中直接刷新模型。

路径规则：

- 本地路径必须是明确的绝对路径；
- UNC 路径必须识别为网络路径，不得拼接到应用工作目录；
- 相对路径只能在明确给出基准目录的接口中解析；
- 目标目录不存在、权限不足、部分写入和最终产物校验失败都返回失败；
- 成功信号只能在实际输出目录、数量和关键内容校验通过后发出。

### 6.4 DataIO 与 GeometryKernel

DataIO 保留格式扩展点和工厂。格式 Adapter 只负责解析、序列化和格式校验；批次、取消、进度、输出校验和生命周期由 DataTransfer 统一协调。

`GeometryKernel` 统一提供：

- rectangle 四点顺序、闭合规则、边界裁剪和退化判断；
- Mask 二值化、尺寸映射、轮廓提取和连通区域；
- 多边形合法性、自交检查和精度规则；
- resize、center crop、padding 及模型坐标到原图坐标的反映射。

LabelMe rectangle 只有在确实能得到四个顶点时才转换。连通区域不按面积提前过滤，只有轮廓少于三个点或退化到无法形成多边形时才丢弃；后续过滤由代理模型或展示投影处理。

### 6.5 ModelWorkspace

`ModelStorageService` 只负责统一路径规则和文件 Adapter；`ModelLifecycle` 负责跨文件和数据库的创建、复制、重命名、删除与恢复；`ModelManager` 负责 Qt Model 和 QML 投影。

所有模型操作必须明确：

- 模型记录、模型目录、`model.db` 和任务目录的所有权；
- 复制训练参数、数据集配置、任务索引和产物的范围；
- 重命名时目录名、数据库记录和内部引用的协调方式；
- 文件占用、权限失败、复制中断和恢复状态的结果表达。

ModelManager 不再拼装多个半成功步骤；确认 ModelLifecycle 结果后才刷新 QML。

### 6.6 TaskRuntime 与 Python 契约

TaskManager 对外保留 QML 表模型入口，内部按以下职责组织：

- `TaskStateStore`：唯一处理事件和合法状态迁移；
- `TaskProtocolAdapter`：C++ 值对象与 Python 消息的编解码和校验；
- `TaskTableModel`：role、排序、过滤和展示派生；
- `TaskArtifactStore`：任务目录、预测产物和持久化状态。

C++ 与 Python 共用一份契约，覆盖消息类型、任务身份、项目身份、状态、进度、错误、耗时、产物引用和终态。边界直接拒绝缺失身份、未知状态、非法进度、类型不匹配、终态后的事件、无法解析的消息和不属于当前任务的结果。

### 6.7 EvaluationEngine

评估保留 `IEvaluationEngine`、方法注册表和 `EvaluationResult`。阈值候选、匹配、指标、图表和视觉派生作为纯值内部 seam，不把算法实现扩散到 ViewModel。

统一数据流：

```text
任务参数快照 + 项目数据快照 + pred/*.tiff
  -> 预测读取
  -> 指标、匹配和阈值计算
  -> EvaluationResult
  -> 可见实例的按需视觉派生
```

评估规则：

- inference 参数改变预测文件或推理输入，启动新的推理；evaluation 参数只重新解释已有预测，不重复推理。
- 没有预测产物时不启动评估；评估参数修改不能隐式创建预测。
- 一次评估内完成阈值搜索、首次应用、指标计算和结果刷新，不触发第二次评估，不引入 revision。
- 每个测试任务第一次成功评估时可自动应用最佳阈值；后续只展示推荐值并保留用户当前值。
- 使用全部有限、去重的预测分数作为真实切分点，精确求解整体 micro-F1、精确率和召回率；并列时选择较高阈值。异常检测、目标检测和语义分割统一遵守候选点规则，匹配规则由评估方法决定。
- 后端保存精确结果；前端按现有规则采样约 100 个曲线点并额外展示最佳点，采样不改变最佳值。所有 tooltip 的数值统一显示四位小数，并显示阈值、F1、精确率和召回率等必要字段。
- 异常检测分数分布保留正常/异常两组。最佳阈值、正常最大分数和异常最小分数只通过 tooltip 区分，不增加额外图例；tooltip 显示“分数”和“数量”。
- 目标检测的 max_det 及推理阶段 conf/iou 属于 inference；已有预测上的 conf 过滤和评估匹配 iou 属于 evaluation。参数名称包含“阈值”不决定分组，实际是否改变预测文件才决定。

性能规则：

- 图像级评估只保留必要统计和轻量索引，不无条件物化全部 TIFF 像素。
- 不将数亿个浮点像素逐个转文本后拼接 SHA-256；常规失效使用规范化路径、文件大小、修改时间、文件身份和任务产物状态等轻量身份。需要完整性校验时直接处理二进制流。
- 打开评估页面只加载结果、矩阵和必要实例索引；TIFF、热力图和缩略图按可见实例懒加载。
- 对相同任务、图像、预测产物和视觉参数只生成一次；阈值未变化不重复生成；重新推理后旧视觉派生整体失效。
- 评估和视觉生成可取消，任务级视觉生成串行化，不无限创建并行任务，不阻塞 UI。

### 6.8 异常区域、热力图和坐标

- 原始 `pred/<image>.tiff` 只读，是分数唯一来源。
- 每个满足几何条件的连通异常区域生成一个多边形，全部叠加；不按面积过滤，少于三个点的轮廓才丢弃。
- 多边形始终显示，复用检查页公共多边形组件，保持原有红色描边，不增加白色描边或第二套样式。
- 热力图位于原图之上、多边形之下，由 `QuiToggleSwitch` 控制；关闭时使用原图坐标系，打开时使用模型输入坐标系下的预处理图像、原始分数和多边形。
- 同一 preprocessing transform 负责 resize、center crop、padding 和模型坐标到原图坐标的反映射；QML 缩略图尺寸只影响渲染，不改变 C++ provider 的坐标数据。
- 生成中保留已有可用结果；首次生成未完成时显示 BusyIndicator；失败时回退原图，保留多边形和评估数值，不清空评估结果。
- 开关切换只改变展示，不重复计算；热力图阈值未变化不重新生成；重新推理后按统一任务生命周期使旧结果失效并重新生成。

### 6.9 FeatureWorkspace

搜索、ROI 搜索、图像聚类、ROI 聚类、智能标注和小样本学习只依赖最小能力接口：数据快照与查询、过滤与结果写回、模型任务提交和任务状态观察。

聚类目标数据集的创建、任务、文件、数据库写入、网络路径、取消、失败回滚和最终刷新由 feature 协调器统一完成。QML 不同时修改数据集、任务和状态，也不在 UI 线程执行图像聚类。

### 6.10 参数、配置与 QML

- `config/models/` 是参数元数据、运行时键、分组、默认值和中文说明的唯一配置入口。
- `name_en` 是持久化和运行时键；`name_cn` 必须按 EasyTrain、Anomalib、Ultralytics 实际代码含义审查，不能从字段名猜测。
- 默认值优先采用框架实际合理默认值；项目特定值明确写入 YAML；不增加 `auto` 隐含状态。
- 数据加载进程默认使用 2，允许范围按实际框架能力设置；batch 默认值保持现有约定。
- 测试参数只保留 `inference` 和 `evaluation` 两组；内部能力不加入普通测试评估参数。
- QML 只负责布局、绑定、轻量交互和结果呈现，不负责数据库事务、协议解析、坐标反映射或评估算法。
- 参数、Busy、进度、耗时、停止按钮和 InfoBar 都由统一任务状态模型派生。
- 复用 `QuiComboBox`、`QuiToggleSwitch`、检查页多边形组件和 `ModelFormDialog.qml` 的弹窗风格；图表切换使用紧凑下拉或互斥控件，不用大尺寸 tab 挤压绘图区。

## 7. 构建、环境与可观测性

### 7.1 CMake

- 使用 `target_link_libraries` 表达真实依赖 DAG，配置阶段发现不存在的 target 和反向依赖。
- 公开头文件只包含调用者需要的类型，具体实现尽量前向声明或移入 `.cpp`。
- `AddTest.cmake` 统一普通测试、QML 测试和项目级测试的注册、环境、标签、fixture 和工作目录；测试源使用 `GLOB CONFIGURE_DEPENDS` 时必须保持目录约定清晰。
- 测试可执行文件只能通过 CTest 启动；Python 工具只负责构建、环境和筛选，不能绕过 CTest 直接启动测试程序。
- 使用 imported target 前必须确保依赖包已提供该 target。InferRT、ONNX Runtime、FAISS 等缺少目标时配置期明确失败，不在下游伪造空 target。
- 同时验证构建树和安装树，覆盖头文件、库、DLL、QML 模块、配置文件和 imported target。

### 7.2 环境和发布

- 开发机绝对路径不能成为跨机器默认事实；Python、CUDA、InferRT、OpenCV 和模型框架通过命令行、环境变量或本地配置注入。
- 项目级测试可在启动时指定 Python 环境目录，并把同一环境传给 CTest fixture。
- 发布脚本统一检查版本、依赖、Qt/QML 运行时、DLL、重解析点和安装树可运行性。
- UNC 路径、临时目录、权限和网络共享必须有明确诊断，不把路径错误归因于业务成功。

### 7.3 日志

结构化日志至少关联 project_id、task_id、阶段、输入规模、输出规模、耗时和状态。长任务只在阶段边界和结束输出摘要；高频进度使用进度通道，不通过逐项日志实现可观测性。

评估定位性能时可暂时保留阶段级 debug 统计，覆盖图像读取、预测读取、指标、视觉派生、结果发布和 UI 聚合；定位完成后关闭高频诊断，仅保留整体评估耗时的 `info` 摘要。

## 8. 分阶段实施路线

每个阶段都必须保持可编译、可运行；先测试，再改实现；一次只跨一个真实 seam；新路径稳定后删除旧路径。

### 阶段 0：事实源和回归基线

工作：审查 CMake target DAG、公开头、安装导出、DDL/运行时 SQL、后台入口、关闭等待点、CTest 标签和项目级 fixture。

出口：Release 可配置和构建；普通、Model/QML、项目级分层和 full 流程均能由 CTest 选择；构建树/测试环境问题有明确诊断。

### 阶段 1：线程边界和输入快照

工作：完善导出快照、评估数据集/图像尺寸/参数快照、导入批次结果、聚合输入和任务句柄注册；移除 worker 对 manager、Qt Model 和数据库连接的捕获。

出口：后台只消费纯值；快照独立性和结果身份校验有行为测试；项目关闭能等待现有句柄。

### 阶段 2：Schema 和强类型数据库

工作：确定唯一 schema 正本；实现空库初始化、逐级迁移、未知版本拒绝、事务回滚和结构校验；建立领域 Record、Result 和批量接口。

出口：业务层不拼 SQL；`:memory:` 测试覆盖初始化、迁移、批量写入、错误和回滚。

### 阶段 3：ProjectContext、TaskRuntime 和关闭栅栏

工作：收敛应用/项目作用域；建立状态存储、协议 Adapter、产物存储；统一停止、取消、耗时、进度、终态和外部进程结束；实现关闭顺序与迟到回调丢弃。

出口：两个项目互不污染任务和缓存；重复终态、停止、关闭、进程异常和迟到消息有测试。

### 阶段 4：DataWorkspace、DataTransfer 和 GeometryKernel

工作：统一数据操作快照、互斥、回滚、导入/导出进度和最终产物校验；完成 DataIO 格式 Adapter 的职责收敛；统一 rectangle、Mask、多边形、resize、crop、padding 和反映射。

出口：导入、导出、取消、部分失败、UNC 路径、损坏输入、单像素区域、LabelMe rectangle 和尺寸差异可独立验证。

### 阶段 5：ModelWorkspace 和跨介质生命周期

工作：实现 ModelLifecycle、ModelStorageAdapter 和操作记录；使用 staging/quarantine 收敛创建、复制、重命名、删除和恢复；删除不可重建的重复投影。

出口：目标存在、权限失败、文件占用、复制中断、数据库失败、重复请求和重启恢复都有测试。

### 阶段 6：EvaluationEngine 和视觉派生

工作：快照化评估输入；拆分预测读取、指标、阈值、图表、实例索引和视觉派生；删除全像素文本哈希；实现预测复用、懒加载、可取消、有界缓存和失效规则；统一异常区域与坐标显示。

出口：evaluation 不重复推理；无预测不评估；相同输入不重复生成；大 TIFF 不造成无界内存或 UI 阻塞；热力图、多边形和评估数值在失败/取消/重新推理后行为一致。

### 阶段 7：Feature、配置和 QML 投影

工作：为 feature 定义最小能力接口；统一 YAML 参数、框架默认值、名称和说明审查；统一 inference/evaluation 语义、任务中禁用编辑、Busy、进度、耗时、停止、失败恢复和图表控件。

出口：feature 可用 Adapter 独立测试；配置只改一处；QML 不含业务算法、协议和持久化。

### 阶段 8：构建、安装和持续验收

工作：修复 imported target、安装导出、DLL/QML 运行时和 CMake 诊断；移除个人路径默认值；补充损坏数据、权限、网络共享、大文件和取消压力夹具；更新 docs 导航。

出口：构建树、安装树、CTest、普通测试、Model/QML 测试、项目级分层和 full 流程在可注入环境中可重复执行。

## 9. 测试体系与验收矩阵

### 9.1 测试分层

| 层级 | 关注内容 | 资源 |
| --- | --- | --- |
| 纯值 | 几何、阈值、PR/micro-F1、匹配、状态迁移、协议编解码 | 内存值对象，通过 CTest |
| 数据库 | schema、迁移、事务、Record、批量写入 | `:memory:` 或临时 SQLite |
| 领域模块 | DataTransfer、ModelLifecycle、TaskRuntime、Feature | 临时文件系统和可替换 Adapter |
| C++/QML | ViewModel、状态、懒加载、失败回退、控件投影 | CTest 注入 Qt/QML/DLL |
| 普通集成 | data/model/UI 的真实模块组合 | 构建资源和临时目录 |
| 项目级 | `.dlpro`、数据导入导出、模型增删改拷、PatchCore 训练/预测/评估 | `tests/assets`、可配置 Python、CTest fixture |
| 压力 | 大量 TIFF、取消、网络路径、迟到事件、关闭收敛 | 明确规模和失败注入 |

普通测试不依赖项目级 `.dlpro`、用户权重或本机 Python。项目级只有 `full` 模式负责从空目录按 fixture 执行完整链路；非 full 层默认复用现有产物，不隐式补齐前置条件，缺少前置产物必须由当前层失败。所有测试由 CTest 启动。

### 9.2 必测不变量

- 失败或取消不会返回与数据库、文件系统和模型内容不符的完全成功。
- schema 只有一个正本；未知版本拒绝；迁移失败回滚。
- worker 不访问 GUI 对象、Qt Model、跨线程连接或裸 manager 指针。
- 项目之间不共享任务、进度、评估缓存和数据库连接。
- 终态不可被迟到事件改变；关闭后没有回调发布到旧项目。
- UNC 路径不会拼到应用工作目录；导出成功可由实际产物验证。
- 推理参数改变才启动推理；评估参数只在已有预测上重新评估。
- 原始 TIFF 不被修改；热力图和多边形可取消、失败回退和重建。
- 相同预测及视觉参数不重复生成；懒加载不阻塞 UI。
- 异常检测保持正常/异常两组分数分布，最佳阈值与指标来源一致。
- 最佳阈值使用全部有限、去重分数求解全局 micro-F1，前端采样不改变后端结果。
- Model 测试脚本和 CTest 标签覆盖所有评估及行为测试。
- 空项目、复用项目、显式重建和 full 模式的前置条件语义互不混淆。

### 9.3 验收顺序

每个阶段至少执行：

1. 对应纯值或领域行为测试；
2. CMake configure；
3. Release build；
4. 相关 CTest 目标或标签；
5. 需要真实资源时执行项目级分层测试；
6. 记录成功、失败、取消、耗时和未验证项。

整体验收顺序：

```text
configure
  -> Release build
  -> ordinary CTest
  -> model/QML CTest
  -> project-layer 单层验证
  -> project-layer full
  -> 安装树消费方验证
  -> 桌面启动烟测
```

性能验收记录输入规模、阶段耗时、内存峰值、缓存命中、UI 线程响应和取消收敛时间，不以固定代码行数或固定数据量作为架构质量指标。

## 10. 迁移与提交纪律

- 先写能观察行为的测试，再移动实现；测试穿过深模块接口，不绑定内部文件布局。
- 一次只跨一个真实 seam；重构后删除旧路径和重复实现。
- 存量数据库需要变更时执行一次性迁移；完成后删除旧入口，不增加隐式兼容分支。
- `QVariantMap` 只存在于 JSON、图表和 QML 动态边界，进入领域层立即转为强类型值。
- 不为每张表、每个页面或每个函数增加只有一层转发的 facade。
- 不把数据库、任务协议、数据格式和项目生命周期搬进 `common`；只有真正跨模块复用的基础设施才进入其中。
- 不用加锁、同步回调、缓存字段或高频日志掩盖对象归属和事实源问题。
- 每个逻辑提交只包含本阶段代码、测试和必要索引文档，不混入用户已有改动。
- 每阶段结束按 `WORKLOG.md` 规则记录目标、状态、验证证据和下一步；仅维护文档时不制造无意义的进度记录。

建议提交顺序：

1. 构建拓扑、公开头和测试选择；
2. 导出/评估输入快照；
3. 任务句柄、取消、关闭和迟到回调；
4. schema、Record 和数据库事务；
5. DataWorkspace、DataTransfer 和 GeometryKernel；
6. ModelLifecycle 和跨介质持久化；
7. TaskRuntime、Python 契约和参数；
8. EvaluationEngine、阈值搜索和视觉懒加载；
9. FeatureWorkspace、QML 投影和构建发布；
10. 删除旧路径并完成全量验收。

每个提交都应可构建、可测试、可恢复，不以一个大提交隐藏多个未验证的结构变化。

## 11. 明确不做

- 不把桌面应用拆成网络服务、事件总线或大量只转发调用的浅模块。
- 不为每张数据库表机械创建公开 Repository。
- 不保留旧任务参数组、旧评估 revision、旧 SQL 入口或兼容双轨。
- 不增加热力图/多边形的第二套持久化格式，不修改原始 `pred/*.tiff`。
- 不使用全量浮点文本序列化、无界内存缓存或高频日志制造完整性假象。
- 不让 QML 负责数据库事务、协议解析、坐标反映射、训练、推理或评估计算。
- 不修改 Anomalib 内部实现来迁就 UI；一致性问题在评估输入和展示派生层解决。
- 不把 2000 行、类数量、目录数量或测试数量当成架构质量本身。
- 不为了兼容旧任务保留已经明确废弃的字段、分组、revision 或状态。

## 12. 最终完成定义

整体改进只有在以下条件全部满足时才算完成：

1. 领域依赖 DAG、数据库 schema、任务协议、参数 YAML、预测产物和测试注册各自只有一个事实源。
2. 所有耗时操作都有纯值输入、实际任务句柄、取消令牌、进度和生命周期校验。
3. 项目关闭和任务停止能够等待所有执行者收敛，迟到回调不会访问旧对象。
4. 数据库事务、文件系统操作和模型生命周期的成功、失败、取消和待恢复语义一致且可测试。
5. DataManager、DataIO、TaskManager 和 FeatureManager 的拆分均对应真实职责或测试 seam，没有浅层包装。
6. C++/Python 协议、任务状态、运行时间和任务产物在重新打开项目后可恢复。
7. 评估复用已有预测；阈值搜索、micro-F1、PR、分布图、热力图、多边形和缩略图具有明确的坐标、懒加载、缓存失效和失败回退规则。
8. 普通测试、Model/QML 测试、项目级分层测试和 full 流程全部由 CTest 可重复执行，缺少前置条件不会被 fixture 静默补齐。
9. Release 构建、安装树和可配置的 Python/InferRT/Qt 环境可验证，缺失依赖给出明确诊断。
10. `docs/` 只承担稳定索引和架构约定，代码、CMake、DDL、YAML 和 CTest 继续作为唯一事实源。

完成定义不以固定耗时或文件行数作为替代指标，必须由行为测试、真实资源流程、失败注入和可观测性证据共同证明架构已经收敛。
