# DeepLearningTool 架构改进最终方案

## 0. 文档定位与适用范围

本文是三份方案合并后的最终实施方案，负责说明目标架构、迁移顺序、接口不变量和验收标准。它不是运行时规则的第二套定义，也不替代源码、CMake、DDL、YAML 和 CTest 的事实源。

事实源索引：

- 模块依赖、构建选项和安装导出：[`CMakeLists.txt`](CMakeLists.txt)、[`src/CMakeLists.txt`](src/CMakeLists.txt)、各模块 `CMakeLists.txt`。
- 公开接口、所有权和线程归属：`src/**/include/` 下的头文件及实现。
- 数据库表、字段、约束和迁移：[`src/database/include/database/ddl/`](src/database/include/database/ddl/)。
- 模型参数、分组、默认值和说明：[`config/models/`](config/models/) 及实际框架实现。
- 测试注册、环境和执行：[`tests/`](tests/)、[`cmake/AddTest.cmake`](cmake/AddTest.cmake)、[`tools/run_model_tests.py`](tools/run_model_tests.py)、[`tools/run_project_tests.py`](tools/run_project_tests.py)。
- 稳定架构、数据存储和测试约定：[`docs/README.md`](docs/README.md)、[`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)、[`docs/DATA_MODEL.md`](docs/DATA_MODEL.md)、[`docs/TESTING.md`](docs/TESTING.md)。

`luna_final_plan.md`、`gemini_final_plan.md` 和 `musespark13_final_plan.md` 是本方案的审查依据；本文件是后续实施的统一方案。方案尚未实施，不表示任何阶段已经完成。

## 1. 最终决策

### 1.1 保留有效技术主干

继续使用 Qt 6/QML、C++、SQLite、CMake 和 Python/EasyTrain。保留以下已有结构：

- `Project` 作为项目级所有权根；
- `IEvaluationEngine` 的公共评估骨架和方法注册表；
- `EvaluationResult` 作为跨线程纯值结果；
- `DataIO` 已有的 `COCOIO`、`LabelMeIO`、`MaskIO`、`FolderIO` 多态实现；
- `ModelStorageService` 的路径规则和目录布局职责；
- `AddTest.cmake`、普通测试/项目级测试分离、CTest 执行和项目级分层脚本。

不进行没有明确收益的全面重写，不把现有有效扩展点替换成一组浅层转发类。

### 1.2 四条不可破坏的架构原则

1. **单一事实源**：定义、运行状态、原始产物和展示派生数据各有唯一权威来源，其他位置只能是可重建投影。
2. **深模块**：复杂行为集中在少数有深度的模块中，调用方只依赖少量稳定接口；接口同时说明类型、顺序、错误、取消和性能契约。
3. **快照跨线程**：后台只消费不可变纯值、绝对路径、取消令牌和显式 Adapter，不捕获 GUI 对象、Qt Model 或项目 manager 指针。
4. **按变化轴拆分**：只有职责混杂、层次错误、领域依赖、变化轴或测试 seam 真实存在时才拆分；代码行数仅作为审查信号，不是机械红线。

### 1.3 风险优先级

| 优先级 | 必须解决的问题 | 目标 |
| --- | --- | --- |
| P0 | 后台越界读取、取消和项目关闭不收敛 | 任务使用快照和句柄，关闭等待实际任务，迟到事件不能访问已释放对象 |
| P0 | 模型目录、项目数据库、模型数据库多写 | 以生命周期深模块协调 staging/quarantine、事务和恢复状态 |
| P0 | schema 重复维护且没有迁移状态机 | 采用一个版本化 schema 正本，迁移失败可回滚，未知版本直接失败 |
| P0 | 应用全局状态与项目状态混用 | 分离 `ApplicationContext`、`ProjectContext` 和 `TaskScope` |
| P0 | 任务定义、运行状态、预测和展示结果多处投影 | 明确每类数据的权威来源，删除不可重建的重复写入 |
| P1 | `DataManager`、`DataIO`、数据库和 `TaskManager` 变化轴混杂 | 内部建立深 seam，物理拆分实现，保持对外接口简洁 |
| P1 | C++/Python 协议字段和终态语义漂移 | 一份契约、双端强校验、边界 fail-fast |
| P1 | feature 依赖完整 manager | 以最小能力接口隔离搜索、聚类、智能标注和小样本任务 |
| P2 | 配置、依赖、CMake、测试和日志存在事实分叉 | 环境可注入、构建可诊断、测试可分层、日志可观测 |

## 2. 当前架构判断

### 2.1 已有优势

- 项目聚合和 QML 装配方向正确，页面不直接管理数据库连接。
- 数据导入导出已经有多态格式实现，问题主要是实现文件 locality、几何规则复用和线程边界，不是缺少格式扩展点。
- 评估模块已经有公共模板、方法注册、强类型结果和 QML 展示注册，适合继续深模块化。
- 模型任务准备、外部 Python 运行器、TCP 事件和任务中心已经形成可运行链路。
- 项目级测试拥有真实 `.dlpro`、夹具、训练/预测/评估流程和可配置 Python 环境，为重构提供了真实验证入口。

### 2.2 结构风险及处理边界

| 风险 | 现象 | 处理边界 |
| --- | --- | --- |
| 线程边界 | `DataManagerDatasetExportSource` 在 worker 中读取 manager；评估尺寸 provider 捕获 `DataManager*`；部分过滤通过 `DirectConnection` 访问 GUI 对象 | 用 GUI 快照替代回读，不以增加锁和同步调用掩盖对象归属问题 |
| 关闭时序 | 全局线程池、评估专用线程池、独立数据线程的等待点不统一 | 由任务句柄和统一关闭栅栏等待实际执行者，禁止只等待一个线程池 |
| 数据库写入 | 导入批量写库可能回到 GUI 线程 | 后台使用线程归属正确的 SQLite 连接和事务，GUI 只应用结果 |
| 模型生命周期 | 文件系统和 SQLite 无分布式事务，现有补偿可能返回与磁盘状态不符的成功 | staging/quarantine + 操作记录 + 可恢复结果 |
| schema | DDL、sqlpp11 表定义和运行时 SQL 重复 | 选定版本化正本，其他内容生成或校验 |
| 任务身份 | `TaskManager`、`model.db`、`task.db`、项目 `extra_data` 各自保存部分状态 | 任务定义和运行状态归任务作用域，项目页只读可重建摘要 |
| 大文件组织 | `DataManager`、`DataIO`、数据库读写和任务表模型承担多种变化轴 | 只按真实变化轴建立内部 seam，不按行数机械切分 |

## 3. 目标架构

### 3.1 作用域和所有权

```text
ApplicationContext
├── 全局设置与最近项目历史
├── 应用级日志和基础设施
└── 应用级 UI 装配

ProjectContext
├── ProjectDataBase
├── DataWorkspace
├── ModelWorkspace
├── ProjectTaskRuntime
└── FeatureWorkspace

TaskScope
├── immutable request / parameter snapshot
├── cancellation token
├── progress channel
├── TaskStateStore
└── Result / ArtifactReference
```

约束：

- `ApplicationContext` 不持有项目数据库、项目数据快照或项目任务运行状态。
- `ProjectContext` 负责项目内对象创建、依赖注入、任务路由和关闭顺序。
- 每个耗时操作拥有独立任务 ID、取消令牌、进度通道和日志上下文。
- 设施型无状态单例可以保留；有状态业务单例必须迁移到显式上下文。
- `TaskManager` 可以继续作为 QML 表模型入口，但不再作为业务代码查找全局状态的方式。

### 3.2 领域依赖 DAG

```text
common / core / database / ui / parameter / settings
                         │
               ┌─────────┴─────────┐
               │                   │
             data                model
               │                   │
               └────────┬──────────┘
                        feature
                          │
                        project
                          │
                         tool
```

这是领域方向，不替代 CMake 的实际 target 依赖。`feature` 可以同时依赖 `data` 和 `model`；不得为了文档简化成错误的线性依赖。构建目录顺序、公开头文件包含和 `target_link_libraries` 必须共同通过依赖检查。

### 3.3 深模块与 Adapter

| 深模块 | 接口职责 | 内部实现/Adapter |
| --- | --- | --- |
| `ProjectContext` | 项目初始化、所有权和关闭 | 数据库、文件系统、任务执行器 |
| `SchemaMigration` | schema 初始化、版本迁移和校验 | SQLite 连接 Adapter |
| `ModelLifecycle` | 创建、复制、重命名、删除和恢复 | `ModelRecordStore`、`ModelStorageAdapter` |
| `DataWorkspace` | 纯值快照、数据操作和展示投影 | `ProjectDataBase`、Qt Model、文件系统 |
| `DataTransfer` | 导入导出、批次、取消、进度和最终结果 | DataIO 格式 Adapter |
| `TaskRuntime` | 状态迁移、协议、取消和结果持久化 | Python 进程、TCP、任务数据库 |
| `EvaluationEngine` | 预测读取、评估、阈值和结果构造 | TIFF、项目快照、纯值算法 |
| `FeatureWorkspace` | 搜索、聚类、智能标注和小样本编排 | 数据/模型/任务最小 Adapter |

只有生产实现和测试替身同时存在，或确实存在两个生产实现时，才建立公开 Adapter seam。单一实现优先作为深模块的内部 seam。

### 3.4 统一数据流

```text
QML / Qt Model
      │
      ▼
项目级深接口生成纯值 Command
      │
      ▼
TaskScope + 后台模块
      │
      ▼
Result + ArtifactReference
      │
      ▼
GUI 线程一次性应用可重建投影
```

后台输入不得包含 `QObject*`、Qt Model、数据库连接或未经所有权约束的 manager 指针。后台结果不能直接修改 QML 状态；所有结果都要经过任务身份、取消状态和项目生命周期校验。

## 4. 单一事实源和持久化设计

### 4.1 权威关系

| 内容 | 唯一事实源 | 派生投影 |
| --- | --- | --- |
| 项目、数据集、图片、标注、类别、Tag、模型身份 | 项目 `.dlpro` | Qt Model、过滤、统计和缓存 |
| 模型参数、模型级数据集配置 | 模型 `model.db` | 参数模型和任务准备快照 |
| 单次训练/测试定义、参数、数据集选择和运行状态 | 任务目录 `task.db` | `TaskManager` 行、项目页摘要 |
| 活跃任务的状态迁移 | 项目内 `TaskStateStore` | 写入 `task.db` 的持久状态 |
| 单次任务原始预测 | 测试任务目录 `pred/` 及其索引 | 评估结果和视觉派生结果 |
| 评估指标、图表和实例数据 | 当前 `EvaluationResult` | QML 图表、矩阵和实例模型 |
| 热力图、多边形、缩略图 | 预测与参数派生缓存 | 内存缓存，不修改原始 TIFF |

规则：

- `TaskStateStore` 是唯一接受状态事件并执行状态迁移的模块；任务列表是它的展示投影。
- `task.db` 是重新打开项目后恢复单次任务定义和运行结果的持久事实源；`model.db` 只保留任务索引和模型级数据。
- 项目模型 `extra_data` 不再作为任务状态、评估应用标记或预测事实源；需要显示摘要时从任务数据库重建。
- 展示结果和缓存丢失后必须能够从预测文件、任务数据库和项目快照重建。

### 4.2 Schema 正本和迁移

采用版本化 migration SQL 作为 schema 正本，sqlpp11 表类型和运行时 schema 校验由正本生成或由构建检查。禁止继续手工维护 DDL、`SqlDef.h` 和表类型三套结构。

`SchemaMigration` 必须支持：

- 空库初始化和当前版本标记；
- 已知版本逐版本迁移；
- 未知版本直接失败；
- 单次迁移事务和失败回滚；
- 缺表、字段、类型、约束、索引不一致的明确诊断；
- 迁移完成后删除旧建表入口。

数据库接口只暴露领域 Record、批量 Result 和明确错误；业务模块不拼 SQL。数据库连接由所属线程创建和销毁，不跨线程传递。

### 4.3 强类型持久化接口

为数据集、图片、标注、模型和任务建立统一定义源的值类型，例如：

```text
DatasetRecord
ImageRecord
LabelRecord
ModelRecord
TaskRecord
BatchResult<T>
Result<T>
```

Record 负责一组具有关联关系的字段，避免 `ids`、`names`、`colors`、`types` 等平行数组依赖隐含的顺序不变量。`Result<T>` 统一表达成功、失败、部分结果、错误上下文和可恢复状态。旧的 `bool + QString&` 和平行数组只有在对应领域接口替换完成后删除，不保留双轨。

### 4.4 跨介质模型生命周期

SQLite 事务不能覆盖文件系统，因此模型创建、复制、重命名和删除使用可恢复的操作记录：

```text
prepared -> staged -> published -> committed
                    └────────────> needs-recovery
```

统一步骤：

1. 校验模型名、路径、重复项、权限、进程占用和当前生命周期状态。
2. 在 staging 目录创建或复制完整模型数据库和必要文件。
3. 校验 staging 内部 schema、目录结构和关键产物。
4. 写入操作 ID、类型、源、目标、staging/quarantine 路径和状态。
5. 在数据库事务中提交逻辑记录，文件发布使用同卷原子改名或可恢复步骤。
6. 发布完成后标记 `committed`，再刷新 Qt Model。
7. 任一步失败都返回失败或 `needs-recovery`，不得只返回一个与实际介质无关的成功布尔值。

具体语义：

- 创建/复制：只向 staging 写入，校验成功后发布最终目录；失败只清理 staging。
- 重命名：由同一协调器维护目录名、数据库名和内部引用；回滚失败必须进入可诊断状态。
- 删除：先把目录移入 quarantine，再提交逻辑删除，最后清理 quarantine；清理失败不得报告“完全成功”。
- 项目启动时恢复未完成操作；重复请求返回已完成、进行中、失败或待恢复，而不是重复写入。

## 5. 线程、取消和生命周期

### 5.1 线程归属矩阵

| 对象/操作 | GUI 线程 | 后台线程 | 规则 |
| --- | --- | --- | --- |
| QML、QObject、Qt Model | 创建、读取、修改 | 禁止访问 | 先在 GUI 线程提取快照 |
| 项目数据库 | 生成快照/应用结果 | 使用线程归属正确的连接执行批量事务 | 连接不跨线程传递 |
| 文件系统、外部进程、评估计算 | 提交和应用结果 | 执行 | 输入使用绝对路径和纯值 |
| 进度、日志、取消 | 发起/展示 | 发布/检查 | 以任务 ID 关联，不使用单一槽位 |
| 关闭 | 阻止新任务并发起取消 | 响应取消并结束 | 等待真实任务句柄后释放项目 |

### 5.2 必须修复的调用路径

- 导出开始前由 `DataManager` 生成包含数据集、图像、标注、类别和路径的不可变快照；`DataManagerDatasetExportSource` 不再在 worker 中读取 Qt Model。
- 评估开始前把 `GlobalFilter` 状态、图片尺寸和数据集选择固化到评估输入；删除 worker 到 GUI 对象的 `DirectConnection`。
- `image_dimensions_provider` 改为读取尺寸映射或快照，不捕获 `DataManager*`。
- 导入批量写库移动到后台数据库执行上下文，GUI 只接收批次结果、刷新模型和更新进度。
- `ModelTaskController`、`ModelTestTaskManager`、数据操作和聚合任务必须注册实际任务句柄；关闭时逐一取消并等待，不只等待全局线程池。
- 所有 queued 回调携带 `project_id`、`task_id` 和取消/生命周期代数；项目关闭或任务终态后的迟到结果直接丢弃，不访问已释放对象。

### 5.3 统一任务状态

```text
created -> preparing -> running -> stopping -> stopped
                         ├──────> succeeded
                         └──────> failed
```

约束：

- `succeeded`、`failed`、`stopped` 是终态；终态不可被迟到事件重新打开。
- 停止请求必须经过 `stopping`，最终落到 `stopped` 或明确失败。
- 重复终态只产生一次状态持久化和一次 UI 通知。
- 训练、推理、评估、导入、导出和 feature 操作使用同一取消、进度、日志和关闭契约。
- 任务状态、参数编辑状态和 Busy 状态由一个状态模型派生，QML 页面不各自猜测。

### 5.4 进度和日志

- 进度按任务/操作作用域发布，批量任务按批次上报，不能长期停在 0 后直接跳 100。
- 长任务的 `info` 日志只保留开始上下文和结束摘要；结束摘要包含耗时、数量、输出位置、成功/失败和可恢复状态。
- 阶段级诊断使用可控的 debug 日志和节流；不在循环中持续写高频日志。
- 少量数据集日志包含名称，大量数据集使用数量和汇总，避免日志本身拖慢任务。
- 运行时间以 C++ 任务生命周期计算，并按任务状态写入任务数据库；重新打开项目后从持久化状态恢复展示。

## 6. 领域改造方案

### 6.1 Database：正本、Record 和事务

保留数据库模块作为 SQLite/sqlpp11 适配层，业务模块不直接拼 SQL。内部按 schema、连接、事务、领域读写组织实现；是否拆成独立公开模块取决于独立生命周期和测试 seam，不为每张表机械创建 Repository。

数据库接口的深度要求：

- 一次调用完成一个领域意图，而不是暴露 SQL 片段；
- 批量读取返回聚合 Record；
- 批量写入明确原子性、部分成功和错误位置；
- 事务边界在 database 模块内保持；
- 测试可以使用 `:memory:` SQLite 通过同一接口验证。

### 6.2 DataWorkspace：DataManager 深门面

保留 `DataManager` 的 QML 外观，内部建立少数深模块：

1. **`DataSnapshot`**：生成数据集、图片、尺寸、标注、类别和路径的纯值快照。
2. **`DataOperationCoordinator`**：统一数据集/图片/标注/划分操作的状态、取消、互斥和结果。
3. **`DataTransfer`**：统一导入、导出、批次、路径、进度和最终校验。
4. **`DataPresentationState`**：维护过滤、统计和 Qt Model 刷新等派生状态。

所有操作结束后通过单一结果入口更新数据库和展示模型。异常、取消和失败必须释放操作状态，不依赖多个独立布尔值恢复可用性。

路径处理规则：

- 本地绝对路径直接使用；
- UNC 路径识别 `\\server\share`，不得拼接到应用工作目录；
- 相对路径只在明确上下文中解析；
- 目标目录不存在、权限不足、部分写入和最终校验失败均返回失败结果；
- 导出结束时验证实际输出，不以“磁盘曾经写入”代替导出成功。

### 6.3 DataIO：按格式物理拆分，统一几何

保留现有多态接口和工厂，物理组织为：

```text
DataIOBase.*
COCOIO.*
LabelMeIO.*
MaskIO.*
FolderIO.*
DatasetIOGeometry.*
```

`DataIOBase` 负责公共生命周期、取消、进度和批处理；格式实现只负责解析、序列化和格式校验；共享几何模块负责：

- 矩形四点顺序、闭合规则和边界裁剪；
- Mask 二值化、尺寸映射和轮廓提取；
- 多边形合法性、自交检查和精度规则；
- padding、中心裁剪、缩放和模型坐标到原图坐标的反映射；
- 不足三个点的轮廓才视为无法形成多边形，其他小区域不在提取阶段擅自过滤。

导入、评估多边形、热力图叠加和 QML 显示统一使用同一几何规则。禁止在 COCO、LabelMe、Mask 和评估实现中分别复制坐标转换。

### 6.4 ModelWorkspace：模型生命周期与任务目录

`ModelStorageService` 只负责路径规则和文件操作 Adapter；`ModelLifecycle` 负责跨介质生命周期；`ModelManager` 负责 Qt Model 和 QML 适配。

复制模型时必须先决定并固定语义：模型级训练参数、测试任务索引和任务目录是否复制，都由命令结果明确表达；内存对象与数据库不能出现“当前进程有、重启后消失”的差异。任何复制范围不写成隐含行为。

### 6.5 TaskRuntime：协议、状态和任务持久化

`TaskManager` 对外保留 QML 表模型入口，内部建立：

- `TaskStateStore`：唯一处理事件和合法状态迁移；
- `TaskProtocolAdapter`：负责 C++ 值对象与 Python 消息编解码；
- `TaskTableModel`：负责 role、排序、过滤和展示派生值。

C++ 与 Python 使用一份版本化契约，覆盖消息类型、必填字段、类型、任务 ID、状态、进度、错误、产物引用和终态。契约可以由 schema 生成两侧常量，也可以由双端共享校验定义实现，但不得继续让 C++ 字符串和 Python 字典各自演化。

边界直接拒绝：缺失任务身份、未知状态、非法进度、错误类型不匹配、终态后事件和不能解析的消息。协议错误必须关联任务 ID 和阶段，不允许静默忽略。

### 6.6 EvaluationEngine：结果分层和性能

保留 `IEvaluationEngine`、方法注册表和 `EvaluationResult`。只把独立变化的纯值算法放入内部 seam：阈值候选/PR/micro-F1、检测匹配、图表采样、异常区域和热力图派生。

评估数据流：

```text
任务参数快照 + 项目数据快照 + pred/*.tiff
  -> 原始预测读取
  -> 指标/阈值计算
  -> EvaluationResult
  -> 可见实例的按需视觉派生
```

性能和一致性规则：

- 推理参数改变预测产物，重新推理；评估参数只作用于已有预测，不重复推理。
- 没有预测文件时不启动评估；已有预测只重新评估。
- 最佳阈值搜索、推荐应用和结果刷新在同一次评估内完成，不触发第二次评估。
- 评估只需图像级分数时，读取 TIFF 后保留必要统计，不能无条件把所有像素物化为长期内存数组。
- 需要展示具体图像时再按需读取完整分数图；实例图像、热力图和缩略图采用懒加载。
- 预测变化通过规范化路径、文件大小、修改时间、任务产物状态等轻量元数据使缓存失效；禁止将数亿个浮点像素逐个转文本再拼接 SHA-256。只有明确完整性要求时才计算内容哈希。
- 热力图、多边形和缩略图是预测与评估参数的派生缓存，不修改原始 TIFF，不增加第二套持久化格式。
- 同一任务、图像和参数只生成一次；重新推理后旧派生结果整体失效。
- 派生生成失败时保留已有评估数值和可用原图/多边形，不清空整个评估结果；结果发布前校验任务、图像和输入产物身份。
- 热力图使用模型坐标系下与预测一致的输入预处理；原图展示使用同一变换的反映射，padding、裁剪、缩放不能由 QML 临时猜测。
- 多边形始终使用评估结果中的红色多边形组件；热力图位于原图之上、多边形之下，由开关控制。
- 后台视觉生成可取消且不阻塞 UI；UI 只显示 Busy 状态和当前仍有效的资源。

### 6.7 FeatureWorkspace：能力收窄

`FeatureManager` 保留项目级 QML 聚合入口，内部按搜索、ROI 检索、智能标注、图像/ROI 聚类和小样本学习使用最小能力接口：

- 数据快照/查询；
- 过滤和结果写回；
- 模型任务提交；
- 任务状态观察。

聚类目标数据集的创建、失败回滚、取消、数据库写入和网络路径由 feature 深模块统一协调；QML 不同时修改数据集、任务和状态。测试使用内存或临时 Adapter，不启动完整桌面应用。

### 6.8 参数、QML 和配置

- YAML 是参数元数据、分组、运行时键、默认值和中文说明的事实源；C++/QML 不复制这些字段。
- `name_en` 是运行时键；`name_cn` 和中文说明基于 EasyTrain、Anomalib、Ultralytics 等实际代码含义审查，不能由字段名猜测。
- 默认值优先采用框架合理默认值，所有项目特定覆盖明确写在配置中；不增加 `auto` 隐含状态。
- 测试参数保持 `inference` 与 `evaluation` 两组：前者影响预测产物，后者只影响已有预测的解释和指标。
- 任务运行、训练、推理、评估和停止收敛期间，当前任务的参数全部禁用，只保留停止/取消和滚动查看；终态后恢复编辑。
- QML 负责布局、绑定、轻量交互和结果呈现；持久化、协议、状态机、几何和评估计算留在 C++ 深模块。
- 页面统一消费任务状态模型，不自行维护互相矛盾的 Busy、运行时间和进度状态。

## 7. 构建、依赖和可观测性

### 7.1 CMake 与模块卫生

- 以 `target_link_libraries` 生成真实依赖 DAG，配置阶段检查新增反向依赖和目标不存在问题。
- 公开头文件只包含调用者真正需要的类型；具体实现类前向声明或移入 `.cpp`，避免上层被迫直连整条底层库链。
- `AddTest.cmake` 继续统一 CTest 注册、环境修改、资源和标签；测试源使用 `GLOB CONFIGURE_DEPENDS`。
- 统一配置选项名称，避免声明名和检查名不同；废弃未实现或无效的构建选项，而不是保留空开关。
- `find_package` 导出的 imported target 必须在使用前由依赖包正确提供；对 `ONNXRuntime::ONNXRuntime`、`Faiss::faiss` 等可选依赖给出明确发现路径和失败诊断，不在下游伪造 target。
- 构建树和安装树都验证头文件、库、DLL、QML 模块和 imported target；能在构建树运行不等于安装包正确。

### 7.2 环境和路径

- `tools/dependencies.yaml` 不把开发机绝对路径作为跨机器事实；路径通过命令行、环境变量或本地未跟踪覆盖注入。
- Python 环境由项目级测试脚本选择，测试环境、构建环境和产品运行环境不互相写死。
- CUDA、InferRT、OpenCV 和模型框架等可选依赖在 configure 阶段报告是否启用、缺失原因和期望配置。

### 7.3 日志和诊断

结构化日志至少带 `project_id`、`task_id`、阶段、耗时、输入规模和结果状态。长任务只在阶段边界和结束输出摘要，高频进度通过进度通道而不是日志实现。日志级别、文件轮转、UI sink 和外部进程连接失败要有统一归属。

性能排查优先保留阶段总耗时和输入输出规模，禁止恢复“每张图/每个像素一条日志”的诊断方式。需要定位时使用临时可控 debug 采样，问题解决后不保留高频打印。

## 8. 分阶段实施路线

每个阶段先写通过现有公开接口的行为测试，再替换实现；阶段结束后删除旧路径，不保留长期双轨。每个阶段都必须保持可编译、可运行，并且只跨一个真实 seam。

### 阶段 0：事实源、构建和回归基线

**目标**：把后续重构建立在可重复的源码和测试事实之上。

**工作**：

- 生成并审查 CMake target 依赖 DAG、公开头依赖和安装导出。
- 盘点 DDL、sqlpp11、运行时 SQL、任务表和 `extra_data` 投影。
- 盘点所有后台入口、线程池、独立线程、取消入口、回调连接和项目关闭等待点。
- 统一 Model 专用脚本、CTest 标签和行为测试的选择范围。
- 固定测试夹具校验、空项目/复用项目/完整流程的前置条件。
- 为模型生命周期、schema、状态终态、快照隔离和项目关闭补最小失败注入测试。

**出口**：普通测试、Model 测试、项目级分层测试和 `full` 流程都能由 CTest 选择；现状缺口有可复现证据。

### 阶段 1：并发边界、取消和关闭

**目标**：先消除会造成偶发崩溃、UI 假死和关闭逃逸的线程根因。

**工作**：

- 导出和评估入口全部快照化，删除 worker 对 Qt Model、QML 对象和 `DataManager*` 的间接读取。
- 将批量数据库写入放到正确线程的连接和事务中。
- 用任务句柄统一数据、模型、评估和聚合线程的取消及等待。
- 统一 queued 回调的项目/任务身份校验。
- 用单一异步状态机替换 DataManager 的散落运行布尔值。

**出口**：并发修改数据时导出仍使用一致快照；评估中修改过滤不越界；停止 preparing/running 任务可以收敛；项目关闭等待所有实际任务；GUI 不承担大批量写库。

### 阶段 2：值类型、Schema 正本和持久化边界

**目标**：让数据库、任务和文件产物各自只有一个事实源，并消除平行数组、散落 SQL 和隐式持久化。

**工作**：

- 先以当前 schema 为基线建立版本化 migration 正本，明确项目库、模型库和任务库的职责及版本号。
- 由正本生成或校验 sqlpp11 表类型和运行时 schema；删除重复的建表入口、重复字段常量和无迁移状态的初始化路径。
- 引入项目、数据集、图片、标注、模型、任务和产物引用的强类型 Record；统一 `Result<T>`、错误上下文和批次结果。
- 把事务边界、SQLite 连接创建/销毁和批量读写收回 database 模块；上层不拼 SQL、不跨线程传递连接。
- 将任务定义、参数、数据集选择、状态和预测索引的持久化归入任务目录 `task.db`；项目摘要和 QML 行只做投影。
- 对 schema 缺失、字段不一致、未知版本和迁移失败提供明确错误；迁移失败回滚，不继续使用半迁移数据库。

**出口**：空库可初始化，已知版本逐级迁移，未知版本直接失败；数据库测试可使用 `:memory:`；业务模块只依赖领域接口和 Record，不依赖 SQL 细节。

### 阶段 3：ProjectContext、任务状态和关闭栅栏

**目标**：把项目生命周期、任务生命周期和应用生命周期分开，彻底收敛关闭竞态。

**工作**：

- 建立 `ApplicationContext`、`ProjectContext` 和 `TaskScope` 的创建、使用及销毁顺序；`Project` 保持 QML 聚合入口，但不再承担所有业务实现。
- 将任务状态迁移集中到 `TaskStateStore`，为每个任务保存请求快照、取消令牌、进度通道、实际任务句柄和结果引用。
- 统一训练、推理、评估、导入、导出和 feature 操作的状态机、停止语义和终态通知；删除散落的运行布尔值及重复终态处理。
- 项目关闭时先禁止新任务，再发起取消，等待所有实际执行者和外部进程，最后释放数据库、manager 和 QML 对象。
- 所有异步回调携带项目/任务身份和生命周期代数；结果应用前验证身份、终态和输入产物，迟到结果直接丢弃。
- 运行时间由 C++ 任务生命周期计算并写入任务数据库；暂停不作为状态，停止与完成一样是终态。

**出口**：关闭项目、切换项目、删除任务和重复停止在并发场景下都能收敛；不存在后台访问已释放对象；终态只持久化和通知一次。

### 阶段 4：DataWorkspace、DataTransfer 和几何规则

**目标**：把数据读写、展示投影、格式转换和几何变换放在正确的深模块中。

**工作**：

- 保留 `DataManager` 的 QML 接口，内部以 `DataSnapshot`、`DataOperationCoordinator`、`DataTransfer` 和 `DataPresentationState` 承担不同变化轴。
- 导入、导出、移动、复制、删除和划分都从 GUI 线程提取纯值快照；worker 只使用快照、绝对路径和取消令牌。
- 保留 `DataIO` 工厂和格式多态实现，按 Folder、Mask、LabelMe、COCO 组织实现；公共批处理、取消、进度和结果由基类/协调器统一处理。
- 把矩形四点、多边形、Mask 轮廓、尺寸映射、边界裁剪、padding、中心裁剪、缩放和模型坐标反映射统一到几何模块。
- 只有轮廓少于三个点时才判定为无法形成多边形；不在提取阶段按面积或尺寸擅自丢弃连通区域。
- 统一本地绝对路径、UNC 路径和明确上下文中的相对路径解析；导出前检查目标，结束后校验真实产物和数量。
- 批量数据库写入使用后台线程自有连接和事务；GUI 只应用批次结果、刷新投影和显示进度。

**出口**：四种格式使用同一几何和路径规则；导入/导出失败不会报告虚假成功；批量操作不阻塞 GUI，进度分批推进且最终准确到 100%。

### 阶段 5：ModelWorkspace 和跨介质生命周期

**目标**：将模型记录、模型目录和任务产物的生命周期统一协调，避免内存、SQLite、文件系统三者不一致。

**工作**：

- `ModelStorageService` 只负责路径计算和文件操作 Adapter，`ModelLifecycle` 负责创建、复制、重命名、删除、恢复和一致性结果，`ModelManager` 只负责 QML 投影。
- 明确复制命令的范围：模型配置、训练权重、任务索引和任务目录是否复制必须是类型化命令结果的一部分，不由目录递归隐式决定。
- 对创建、复制、重命名和删除统一使用 staging/quarantine、操作记录、原子发布和恢复状态；禁止用单个 `bool` 表示跨介质操作已完全完成。
- 项目启动时扫描并恢复未完成操作；重复操作根据操作 ID 返回已完成、进行中、失败或待恢复，不重复破坏目录。
- 模型名称、目录、数据库名和内部引用由同一生命周期模块校验和更新；旧路径在切换完成后删除，不保留双写。

**出口**：每个模型操作都有可验证的最终状态；文件系统失败能被诊断和恢复；重启后模型列表、数据库和目录一致。

### 阶段 6：TaskRuntime、Python 协议和模型参数

**目标**：让 C++ 与 Python 的任务协议、参数定义和终态行为只有一份可验证契约。

**工作**：

- 以强类型消息定义启动、阶段、进度、日志、产物、错误和终态；协议 Adapter 负责 C++ 值对象与 Python 消息编解码。
- 对消息身份、类型、必填字段、进度范围、错误上下文和终态后事件做边界校验；异常直接报告任务 ID 和阶段。
- `config/models/` 作为参数元数据、运行时键、分组、默认值、名称和说明的唯一入口；参数模型和 QML 不复制 YAML 字段。
- 逐项对照 EasyTrain、Anomalib、Ultralytics 实际实现审查 `name_en`、`name_cn`、默认值、范围和中文简介；不添加猜测性的 `auto` 参数。
- 测试参数固定分为 `inference` 与 `evaluation`：推理参数影响预测产物，评估参数只作用于已有预测；没有预测产物时评估直接失败或不启动，取决于调用契约。
- 任务运行期间当前模型的参数、数据集和评估编辑全部禁用，只保留停止/取消与滚动查看；终态后由状态模型统一恢复。

**出口**：Python 任务可被 C++ 严格解析，非法协议不会静默继续；参数显示、持久化和运行使用同一键和值；推理和评估触发关系可通过行为测试证明。

### 阶段 7：EvaluationEngine 和视觉派生数据

**目标**：在不修改 Anomalib 内部行为和不增加持久化格式的前提下，保证评估性能、指标一致性和实例视觉一致。

**工作**：

- 保留 `IEvaluationEngine`、方法注册表和 `EvaluationResult`，将阈值搜索、匹配、图表、异常区域和视觉派生作为内部纯值 seam。
- 对每个任务收集全部有限且去重的预测分数作为真实切分点，按 micro-F1 求全局最佳阈值；前端曲线仍按现有规则采样约 100 点并额外显示最佳点。
- PR 曲线和悬浮提示提供精确率、召回率、F1 和阈值，数值统一显示四位小数；异常检测分数分布继续区分正常/异常两组，并显示三条阈值线的中文提示。
- 分类阈值用于异常区域分割；每个连通异常区域生成一个多边形，除轮廓少于三个点外不跳过区域。多边形始终复用检查页的红色显示组件，并保留后续代理过滤接口。
- 异常热力图直接使用 `pred/*.tiff` 的原始像素异常分数，不对每张图使用自身最大值归一化；使用模型输入坐标系的预处理结果，原图显示由同一变换反映射。
- 热力图叠加顺序固定为：原图 → 热力图 → 多边形；开关只控制热力图，关闭时显示原图和原图坐标多边形。热力图生成失败时回退原图，保留多边形和评估数值。
- 视觉派生按可见实例懒加载，任务级别串行化生成，阈值不变时复用结果；重新推理后使旧派生结果失效，但不重复推理。
- 图像级评估只保留必要统计和轻量文件身份；禁止把所有 TIFF 浮点像素转文本后生成 SHA-256。若必须检测同大小同时间替换，使用原始二进制流哈希或更可靠的文件身份，且不物化文本副本。
- 评估输入和派生结果发布前校验任务、文件和参数身份；后台计算可取消，不把视觉生成放到 GUI 线程。

**出口**：评估复杂度随样本和必要分数数据增长，不随重复字符串构造增长；打开评估页面不因全部缩略图同步计算而阻塞；热力图、多边形、阈值和指标使用同一坐标与原始分数规则。

### 阶段 8：FeatureWorkspace 和 QML 投影

**目标**：收窄 feature 对完整 manager 的依赖，并让 QML 只承担展示和轻量交互。

**工作**：

- 保留 `FeatureManager` 作为项目级 QML 聚合入口，搜索、ROI 搜索、图像/ROI 聚类、智能标注和小样本学习分别使用最小能力接口。
- 聚类目标数据集创建与任务、文件、数据库写入由 feature 协调器统一完成；失败回滚、取消、网络路径和结果应用走同一任务契约。
- QML 页面只绑定状态模型、Qt Model 和纯值结果，不直接读写数据库、不猜测 Busy/进度/运行时间、不从显示尺寸反推模型坐标。
- 公共控件和页面按现有 `ui`/领域模块归属维护；保持紧凑控件、少量描述文本和统一弹窗风格。
- 统一 ComboBox、开关、只读图表线、缩略图懒加载和任务终态的测试入口，删除无法复用的页面局部状态。

**出口**：feature 操作能够脱离完整桌面应用进行测试；QML 页面只有一条状态来源；模型、数据、评估和聚类页面对运行中状态的处理一致。

### 阶段 9：构建、安装、清理和最终验收

**目标**：删除旧路径和事实分叉，让构建树、安装树、测试树和运行时环境都可重复。

**工作**：

- CMake 以真实 `target_link_libraries` 维护依赖 DAG；公开头文件只暴露必要类型；新增源文件通过 `GLOB CONFIGURE_DEPENDS` 纳入并由分配检查防止漏测。
- `find_package` 使用者在读取 imported target 前确保依赖包已提供目标；InferRT、ONNX Runtime、FAISS 等缺失时给出配置期诊断，不在下游伪造空 target。
- `AddTest.cmake` 统一普通测试、QML 测试和项目级测试的环境、标签、fixture 与工作目录；所有测试通过 CTest 启动，不直接运行可执行文件。
- Python 环境、第三方根目录和构建配置由命令行/环境变量注入；开发机路径不作为跨机器事实。
- 完成迁移后删除旧接口、旧状态投影、旧 SQL 入口、重复几何实现和临时诊断日志；不以兼容层延长双轨。
- 同时验证构建树和安装包：库、DLL、QML 模块、配置、Python 运行时和 imported target 均可被消费方找到。

**出口**：新机器可按文档配置，Release 构建、安装包验证、普通 CTest、Model 测试和项目级 `full` 流程全部可重复执行。

## 9. 测试和验收矩阵

每个阶段都先增加能穿过目标深模块接口的行为测试，再替换实现；测试不通过不得进入下一阶段。测试不模拟已经被绕过的真实路径。

| 领域 | 必须覆盖的行为 | 验收方式 |
| --- | --- | --- |
| 构建/依赖 | target DAG、公开头、安装导出、缺失 imported target | CMake configure、Release 构建、消费方探针 |
| Schema/数据库 | 空库、逐级迁移、未知版本、回滚、事务原子性、线程连接 | `:memory:` 与临时 SQLite 行为测试 |
| 项目生命周期 | 创建/打开/切换/关闭，迟到回调和失败恢复 | 项目级测试 + 失败注入 |
| 数据传输 | Folder/Mask/LabelMe/COCO、UNC、失败校验、批量进度与取消 | DataIO 测试 + 项目级分层测试 |
| 几何 | 矩形、Mask、多边形、裁剪/缩放/padding 反映射 | 固定坐标断言和可视化回归 |
| 任务运行 | 协议解析、状态迁移、停止、外部进程、运行时间持久化 | C++ 行为测试 + Python 合同测试 |
| 模型生命周期 | 创建、复制、重命名、删除、staging/quarantine、重启恢复 | 临时目录和失败注入测试 |
| 评估 | 原始 TIFF、micro-F1、最佳阈值、PR/分布图、缓存失效 | 算法测试 + 真实预测夹具 |
| 视觉派生 | 懒加载、单任务串行、热力图开关、多边形叠加、失败回退 | QML 测试 + 可见缩略图集成测试 |
| feature | 聚类/搜索/小样本学习的快照、取消、写回 | 最小 Adapter 测试 + 真实资源测试 |
| UI | Busy、停止、终态恢复、控件布局和弹窗风格 | Qt Quick Test 与真实桌面烟测 |

### 测试分层规则

- **普通测试**只使用内存或临时目录，不依赖项目级 `.dlpro`、模型权重和用户 Python 环境；通过 CTest 的 `ordinary`、`data`、`model`、`qml`、`tools` 标签选择。
- **项目级测试**使用真实 `.dlpro`、`tests/assets/model` 夹具、可配置 Python 环境和固定项目根目录；每一层只验证本层的前置条件。
- `full` 才负责从空项目根目录按 fixture 顺序完成项目创建、数据创建/导入/导出/划分、模型创建、训练、预测和评估。
- 非 `full` 层默认复用现有产物，不隐式补齐前置条件；显式重新创建项目时清空并重建项目根目录，缺少前置产物必须由当前层失败。
- Windows 测试统一由 CTest 注入 DLL、QML、资源和 Python 环境；不直接启动测试可执行文件。
- 性能验收以阶段耗时、输入规模、内存峰值和 UI 响应为证据，不设置机械的文件行数或数据量红线。

## 10. 变更和提交顺序

按真实 seam 分阶段提交，每个提交都是可构建、可验证的完整状态：

1. `refactor: 收敛构建拓扑与模型测试选择`：构建、测试选择和公开头依赖基线。
2. `refactor: 收敛任务并发与项目生命周期`：快照、取消、关闭栅栏和统一状态。
3. `refactor: 统一数据库事实源与迁移边界`：Record、schema 正本、事务和任务持久化。
4. `refactor: 重构数据传输与几何处理边界`：DataWorkspace、DataIO 和路径/几何规则。
5. `refactor: 收敛模型生命周期与任务协议`：模型跨介质操作和 C++/Python 契约。
6. `perf: 优化评估数据流与视觉派生`：评估、懒加载、缓存和坐标一致性。
7. `refactor: 收窄特征模块与界面状态来源`：feature Adapter 和 QML 投影。
8. `chore: 清理旧路径并完成安装验收`：删除双轨、构建/安装/全链路验收。

提交前固定检查：

- 仅包含当前阶段的代码、测试和必要索引文档；不把用户已有的无关工作区改动带入提交。
- `git diff --check` 通过。
- 对应目标完成 Release 构建和 CTest 验证；未验证的项目级真实流程明确记录为未验证。
- 阶段完成后更新 `WORKLOG.md`，新条目位于固定示例之后且按最新在前排列。

## 11. 不做的事情

- 不把桌面程序拆成网络服务、事件总线或大量只转发调用的浅模块。
- 不为每张数据库表机械创建公开 Repository；只有真实变化轴或测试 seam 才建立模块接口。
- 不保留旧任务参数组、旧评估 revision、旧 SQL 入口或长期兼容双轨；破坏性重构直接使用新契约，未知输入报错。
- 不增加热力图/多边形的第二套持久化格式，不修改原始 `pred/*.tiff`。
- 不用全量浮点文本序列化、无界内存缓存或高频日志制造“完整性”假象。
- 不让 QML 负责数据库事务、协议解析、坐标反映射或耗时评估计算。

## 12. 最终完成定义

当且仅当以下条件同时满足，整体方案才算完成：

1. 领域依赖 DAG、数据库 schema、任务协议、参数 YAML、预测产物和测试注册均只有一个事实源。
2. 所有耗时操作都能在项目关闭和任务停止时收敛，后台不访问 GUI 对象或已释放项目对象。
3. 数据、模型、任务、评估和 feature 的公共接口是深模块接口，调用者不需要了解其内部线程、SQL 和文件系统细节。
4. 普通测试、Model/QML 测试、项目级分层测试和 `full` 流程均由 CTest 可重复执行，并且失败前置条件不会被 fixture 静默补齐。
5. Release 构建和安装包在当前机器及配置注入后的干净环境中可验证；未解决的缺口有测试或明确诊断，而不是兼容性兜底。

### 阶段 2：Schema 正本和强类型数据库接口

**目标**：消除 schema 和数据库出参的结构性分叉。

**工作**：

- 选定版本化 migration SQL 为正本，移除重复建表入口。
- 实现 schema 版本校验、迁移事务和未知版本拒绝。
- 定义 Dataset/Image/Label/Model/Task Record、`Result<T>` 和 `BatchResult<T>`。
- 将平行 vector 和弱错误接口按领域替换为聚合值对象。
- 增加 `:memory:` SQLite 测试，并把事务边界留在 database 模块。

**出口**：空库初始化、已知迁移、未知版本、字段/索引不一致和迁移回滚均可测试；业务模块不拼 SQL。

### 阶段 3：模型生命周期和持久化一致性

**目标**：模型文件、项目记录和模型数据库的结果一致且可恢复。

**工作**：

- 实现 `ModelLifecycle`、`ModelRecordStore` 和 `ModelStorageAdapter`。
- 建立 staging/quarantine 和操作记录。
- 将创建、复制、重命名、删除的补偿逻辑从 `ModelManager` 移入生命周期深模块。
- 统一复制范围和内存/持久化语义。
- 删除模型任务状态在项目 `extra_data` 中的重复写入。

**出口**：目标已存在、数据库失败、磁盘权限失败、文件占用、复制中断、重复操作、重启恢复都有测试；不返回与实际介质不符的完全成功。

### 阶段 4：DataWorkspace 和几何内核

**目标**：让数据操作可快照、可取消、可测试，且所有格式和评估使用统一坐标规则。

**工作**：

- 在 DataManager 内部建立快照、操作协调、传输和展示状态 seam。
- 按格式物理拆分 DataIO，保留既有多态接口和工厂。
- 抽取统一几何模块，覆盖矩形、Mask、多边形、padding、裁剪、缩放和反映射。
- 修复本地/UNC/相对路径、导出最终校验、批次进度和导入写库线程归属。
- 为 LabelMe rectangle、Mask 尺寸差异、单像素区域和损坏输入增加边界测试。

**出口**：导入、导出、取消、进度、格式错误、坐标映射、网络路径和权限失败可以独立验证；QML 不承担数据 I/O。

### 阶段 5：ProjectContext、TaskRuntime 和 Python 契约

**目标**：统一作用域、任务状态和跨进程协议。

**工作**：

- 引入 `ApplicationContext`/`ProjectContext` 显式装配，消除项目打开时清空全局任务的行为。
- 将项目任务、进度和评估缓存从全局单例迁移到项目/任务作用域。
- 建立 `TaskStateStore`、协议 Adapter 和表模型内部 seam。
- 用一份契约定义 C++/Python 字段、类型、进度、错误和终态。
- 将任务状态写入 `task.db`，项目页只读取可重建摘要。

**出口**：两个项目互不清空任务和进度；重复终态、迟到事件、进程崩溃、停止和关闭均遵循同一状态机；协议双端契约测试通过。

### 阶段 6：评估深模块和性能路径

**目标**：在复用已有预测的前提下，让评估、阈值搜索和可视化可取消、可缓存、可懒加载。

**工作**：

- 评估输入使用项目/任务快照，彻底移除 worker 对 GUI 对象的间接读取。
- 将阈值搜索、PR/micro-F1、匹配、图表采样和视觉派生算法做成纯值内部 seam。
- 用预测文件轻量元数据和产物状态做缓存失效，移除像素转文本哈希路径。
- 分离原始预测读取、评估结果、视觉派生和 UI 实例懒加载。
- 统一热力图、多边形、坐标反映射、失败回退、重复生成和重新推理失效规则。
- 添加大规模 TIFF、取消、重复打开、参数改变和生成失败性能测试。

**出口**：评估参数不重复推理；没有预测不评估；同一输入不重复生成；选中实例按需加载；UI 不被评估和可视化阻塞。

### 阶段 7：Feature、配置和 UI 装配

**目标**：收窄 feature 依赖，消除配置和页面状态分叉。

**工作**：

- 为搜索、聚类、智能标注和小样本任务定义最小数据/模型/任务接口。
- `FeatureManager` 只负责项目装配和 QML 入口。
- 统一 YAML 参数元数据、框架默认值、中文说明和 inference/evaluation 分组。
- 统一参数禁用、运行时间、Busy、停止和失败恢复状态。
- 删除已无调用的旧路径、重复字段和状态投影。

**出口**：feature 可用最小 Adapter 测试；配置只改一处；任务运行期间参数不可编辑；QML 不包含业务算法和持久化逻辑。

### 阶段 8：构建、发布和持续验证

**目标**：干净环境下可配置、可构建、可通过 CTest 运行。

**工作**：

- 修复可选 imported target、安装导出、DLL/QML 运行时和 CMake 选项诊断。
- 移除开发机路径作为默认事实，统一命令行/环境覆盖。
- 建立 ordinary、model、QML、project、heavy/pressure 测试矩阵。
- 增加损坏图片/TIFF/数据库、权限、网络共享、大数据量和取消收敛夹具。
- 更新 `docs/` 导航，只保留稳定索引并指向源码事实源。

**出口**：构建树和安装树均可验证；CTest 能区分代码失败、环境缺失和外部资源不可用；长任务日志有界且可定位。

## 9. 测试体系

### 9.1 测试分层

| 层级 | 关注内容 | 资源和 Adapter |
| --- | --- | --- |
| 纯值测试 | 几何、阈值、PR/micro-F1、匹配、状态迁移、协议编解码 | 内存纯值 |
| 数据库测试 | schema、迁移、事务、Record 和批量写入 | `:memory:` SQLite |
| 领域模块测试 | ModelLifecycle、DataTransfer、TaskRuntime、Feature | 临时文件系统、内存 Adapter |
| C++/QML 测试 | ViewModel、注册表、状态、懒加载、失败回退 | CTest 注入 Qt/QML/DLL |
| 普通集成测试 | data/model/UI 真实模块组合 | 构建资源和可控临时目录 |
| 项目级测试 | `.dlpro`、数据导入导出、模型增删改拷、训练/预测/评估 | `tests/assets` 和可配置 Python |
| 压力测试 | 大文件列表、TIFF、取消、网络路径和迟到事件 | 明确规模和失败注入 |

所有测试都由 CTest 启动。Python 工具只负责配置、构建、环境变量和 CTest 筛选，不直接启动测试可执行文件。

### 9.2 必须验证的不变量

- 失败操作不会报告与数据库或磁盘状态不符的完全成功。
- schema 只从正本生成，未知版本拒绝，迁移失败回滚。
- 后台任务不访问 GUI 对象、Qt Model、跨线程数据库连接或裸 manager 指针。
- 项目之间不共享任务、进度、评估缓存和数据库连接。
- 任务重复终态和迟到消息不会改变已完成任务。
- 项目关闭后没有仍可发布到旧项目对象的后台回调。
- UNC 路径不会被拼接到应用工作目录，导出最终结果能从实际目录验证。
- 评估参数只重新评估已有预测；推理参数改变才重新推理。
- 原始 TIFF 不被修改，视觉派生结果可失效、可取消、可重建。
- 同一预测和视觉参数不重复生成，懒加载不阻塞 UI。
- Model 测试脚本和 CTest 标签不遗漏行为测试。
- 项目级测试的空项目、复用项目、显式重建和 `full` 模式前置条件互不混淆。

### 9.3 验收顺序

具体参数、项目根目录、项目名称、数据集名称和 Python 环境以 [`docs/TESTING.md`](docs/TESTING.md) 及脚本事实源为准。实施阶段按以下顺序执行：

```powershell
cmake -S . -B build -DDLT_BUILD_TESTS=ON
cmake --build build --config Release --parallel 4
ctest --test-dir build -C Release -L ordinary --output-on-failure
python tools\run_model_tests.py --skip-build
python tools\run_project_tests.py --project-layer project-creation --skip-build
python tools\run_project_tests.py --project-layer full --skip-build
```

性能验证以阶段 0 的可重复基线为参照，至少记录评估各阶段耗时、读取文件数、物化像素数、缓存命中、UI 线程阻塞和取消收敛时间；不以单个硬编码行数或机器绝对耗时作为架构完成标准。

## 10. 迁移纪律和禁止事项

- 先补行为测试，再移动实现；测试通过深模块接口观察结果，不测试内部文件布局。
- 每次只跨一个真实 seam；重构后删除旧路径，不保留长期双实现。
- 存量数据库需要变换时执行一次性迁移，完成后删除旧入口；不增加隐式兼容分支。
- `QVariantMap` 只允许出现在 JSON、Chart.js 和 QML 动态边界，进入领域层立即转换为强类型值。
- 不为每张表、每个 QML Loader 或每个函数增加只有一层转发的 facade。
- 不把数据库、任务协议、数据格式和项目生命周期搬进 `common`；只有跨模块真正复用的基础设施才进入 `common`。
- 不用加锁、同步回调、缓存字段或高频日志掩盖错误的对象归属和事实源问题。
- 不把代码行数、类数量、目录数量或测试数量当作架构质量本身；以深度、locality、失败语义、可测试性和调用方学习成本验收。
- 不修改 Anomalib 内部实现来迁就 UI；在评估输入和展示派生层解决一致性问题。
- 文档只保留最终决策、稳定索引和验收标准；字段、状态、路径、参数和测试名称始终指向其源码事实源。

## 11. 最终完成标准

只有以下条件全部满足，才认为最终方案完成：

1. P0 的线程、关闭、schema、模型生命周期和任务状态均有失败注入测试及真实路径验证。
2. 数据库、文件系统、任务运行时和 UI 投影的权威关系已落地，重复事实源已删除或可重建。
3. DataManager、DataIO、TaskManager 和 FeatureManager 的每次拆分都对应真实职责/变化轴和测试 seam，没有浅层包装。
4. 所有后台任务都使用纯值快照、任务句柄、取消令牌和生命周期校验。
5. 模型任务、数据导入导出、评估和 feature 操作的错误、取消、进度、日志和关闭语义统一。
6. 评估复用预测产物，阈值搜索、热力图、多边形和缩略图具有明确的懒加载、缓存失效和失败回退规则。
7. C++/Python 协议、YAML 参数和 CMake 依赖没有第二套手工事实源。
8. ordinary、model/QML、project 和完整流程均通过 CTest；压力测试覆盖损坏输入、权限、网络路径和取消收敛。
9. 构建树、安装树和可配置外部环境能够完成 configure、build 和测试，并对缺失依赖给出明确诊断。
10. `docs/` 只承担索引和稳定约定，代码、CMake、DDL、YAML 和 CTest 继续是唯一事实源。

本方案完成的是最终架构决策和执行路线；每个阶段必须以独立代码变更、测试结果和验证证据结项。
